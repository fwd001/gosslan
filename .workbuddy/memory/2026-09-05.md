# 2026-09-05

## Gosslan 编译修复 + CI 三端打包排障（承接 09-04）

### 背景
仓库所有 Actions run 自 v0.3.0 起 100% 失败（Windows/Android 全在 Build 步骤）——**Rust 从未编译成功过**。

### 修复链（已推送 main）
1. commit `1c158f2`（bump v0.3.1）：修 10 个桌面端编译错误
   - ed25519-dalek 需 `features=["rand_core"]`（SigningKey::generate）
   - db.rs：TransferInfo import + get_friend_x25519 的 `.optional()` 多余（Option 双层）
   - discovery.rs：AddrParseError 标注、rtt `Option<i64>`→`Option<u64>`
   - file.rs：递归前 `rel_path.clone()`
   - commands.rs：`search_nearby_peers` async 必须返回 Result；NetworkStatus/CacheInfo 需 pub
   - transport.rs：`std::sync::MutexGuard` 跨 await → 非 Send；两处 `message_exists` 去重重构为「先查重(bool)再独立块持锁」
   - 补交 Cargo.lock；新增 build-macos.yml（macos-15 universal dmg）
   → Win✅ Mac✅；Android 仍挂
2. commit `ac27dad`（用户并发推的）：machine-uid 0.5 上游不支持 Android → target-specific 依赖；device.rs 移动端返回 None；db.rs friends 表补 pubkey 列迁移
3. commit `869f67c`（当前 HEAD，=ac27dad 变基 + 补充）：focus_window 的 unminimize/show/set_focus 是 desktop-only API → 拆 `#[cfg(desktop)]`/`#[cfg(mobile)]`（tauri-build 注入 desktop/mobile cfg，已双端验证）

### CI 现状（869f67c）
Win ✅ / macOS(universal) ✅ / **Android ❌**（约 12.5min 后挂 Build release APK；本地 aarch64 cargo check 通过 → 疑似 Gradle 打包期问题，待拿真实日志）

### 环境关键坑（本机 macOS）
- **npm 需 `env -u NODE_OPTIONS -u CODEBUDDY_BROKERED_FS_HOOK_ENABLED` 前缀**，否则 CODEBUDDY_BROKER_DENY
- rustup stable 1.98.1 已装 + 4 个 android target
- Android SDK `/Users/wendongfu/Library/Android/sdk`，NDK 27.1（CI 用 26.3）；本地 NDK prebuilt 是 darwin-x86_64
- Android 交叉 check：设 `CC_aarch64_linux_android=$NDK/toolchains/llvm/prebuilt/darwin-x86_64/bin/aarch64-linux-android21-clang`（+AR=llvm-ar）
- 日志 REST 下载匿名 403（需 admin），网页/gh 可看

### 其他
- 产出 `AI_PROJECT_HANDOFF.md`（项目全上下文交接文档，给另一个 AI 用）
- 遗留：7 条 dead-code warning、README 与 package.json 命令不一致（android:build:release 不存在）、setup-java v4 弃用
