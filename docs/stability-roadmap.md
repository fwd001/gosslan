# Gosslan 稳定版工作地图（stability-roadmap）

> 本文件是「最终稳定版」阶段的**唯一工作地图**。它的用途不是描述愿景，而是回答三个问题：
> **现在真正被机器证明到哪一步／接下来按什么顺序做／每一项做完的判据是什么。**
>
> 建立于 2026-09-25（总指令§二第一阶段产物）。**本阶段只审计，不改生产代码。**
> 任何与总指令冲突的判断，以总指令为准；任何与本文件数字冲突的**实测结果**，以实测为准并回来改本文件。

---

## 0. 判定口径

**Definition of Done（覆盖一切）**：不是"测试通过"，而是
「用户实际操作路径、边界、异常恢复、跨实例通信、数据一致性、UI 反馈、文档契约
全部能被机器反复验证，并长期保持通过」。

**任务分级**：A 必须修复（用户可感知故障／数据或消息丢失／状态错误／卡死／边界破坏）
／ B 应该优化（结构性风险，暂无现场故障，但做了能明确降低回归概率）
／ C 可以延后（美化、命名、非必要抽象）／ D 不应该做（无需求来源、无证据的大重构、无关依赖升级、重写已稳定机制）。

**处置动作**：保留 / 修复 / 优化 / 延后 / 删除。

**覆盖标注（不允许"没测"冒充 PASS）**：
`AUTOMATED`＝CI 或本地门禁里真的跑；`SIMULATED`＝进程内夹具/假对端跑过，但不是真实进程；
`MANUAL-HARDWARE`＝只能真机或人眼看，明确写进 §6 Smoke 清单。

**任务状态机（§二十一，每条任务只能处在一态，禁止"改完码→DONE"）**：
`DISCOVERED → AUDITED → PLANNED → IMPLEMENTING → UNIT_VERIFIED → INTEGRATION_VERIFIED → E2E_VERIFIED → DOCS_SYNCED → GUARDS_LOCKED → DONE`

---

## 1. Current Architecture Assessment（现状架构判定）

**结论：架构方向正确、层次边界已经收口，且这一层不是当前风险来源。风险集中在"证明的形状"——
现有自动化几乎全部是**静态源码断言 + 进程内单元**，没有一层真的把应用当应用跑起来。**

### 1.1 分层与依赖方向（实测）

`UI → src/api 门面 → Commands → Domain → Persistence/Network` 成立，且**是机器守着的**：

| 判据 | 守在哪 | 现状 |
|---|---|---|
| 前端只能走 `src/api`（出现 `@tauri-apps/api/core` 即红） | `src/api/events.test.ts` | 绿 |
| 注册表 ↔ 契约图 IPC 表双向等值、两侧无重复 | `src/utils/mapContract.test.ts` | 绿，实测 **129** 条 |
| 门面包装必须被引用；注册表 ↔ 前端调用面逐条对账；死命令必须为 0 | `mapContract.test.ts` / `events.test.ts` | 绿（本轮已删 5 条零引用命令） |
| 发射事件不得持 DB 锁（INV-P25） | `scripts/check-lock-scope.mjs`（全部取锁点逐处扫；条数由脚本自己打印） | 绿 |
| 领域图必须认领每个源文件、一文件一域、`activeHome` 字段齐 | `check-domain-map.mjs` 判据 A–F | 绿 |
| Rust 跨域 `use crate::…` 必须在 `consumes` 里 | `check-domain-deps.mjs` 判据 G–I | 绿（**只扫 Rust，前端不扫**） |
| 不变量例外必须登记 | `check-invariant-exceptions.mjs` | 绿（现 2 条：INV-P03/P04） |

### 1.2 规模（本轮逐个复算，不引用旧文档）

| 事实 | 数字 | 怎么量的 |
|---|---|---|
| 后端注册命令 | **129** | `lib.rs` `generate_handler!` 条目数 |
| 前端测试用例 | **606** | `node --test --test-reporter=tap` 的 `^ok` 计数 |
| Rust 用例基线 macOS | **690** | `src-tauri/test-baseline.macos.txt` 行数 |
| Rust 用例基线 Windows | **679** | 同上 windows（2026-09-26 由 `--sync-baselines` 按源码门控推出；差额 14 条全部有名有据） |
| 护栏非空转用例 | **189** | `verify-guards.py` 的 `Case(` 计数；最新两条是「不变量钩子解析不出来」与「给 Identity 补一行 derive(Serialize)」 |
| 登记不变量 | **26** | `protocol-invariants.md` 的 `^### INV-P` |
| 其中**有具名钩子绑定** | **26 条全部具名**（P18 于同日补上 `check-key-boundary.mjs`） | 由 `scripts/check-invariant-hooks.mjs` 逐条现算并绑定（条数由该步打印） |
| 门禁层 | 步数一律由 `verify.mjs --list` 现算（`check-doc-numbers.mjs` 对账），本行不抄数字；CI 3 job | `scripts/verify.mjs` 步骤表 |
| 真实多实例 E2E | **0** | 见 §5.3 |

### 1.3 结构上的既成事实（保留，不要动）

- `include!` 分册使模块命名空间扁平 ⇒ 守卫必须读**手工聚合视图**（`all_commands_src()` / `all_db_src()` / `transport_src_for_guards()`）。已有"漏登记=假绿"的教训与对应对账守卫，**新增分册必须同步三处登记**。
- 单全局 `Mutex<Connection>` + 「emit 出锁」是已经落地的取舍，不是待改的债。
- 文件链：`.part` 流式 + attempt epoch + `chunk_size` 上线协商（v4.29.41 收口 P4）+ 终态唯一出口 `db::finalize_file_failure`。
- 常驻窗口 = 关即隐藏 ⇒ 自愈契约是「重新可见/重获焦点必拉数据」，已有判据现场读 Rust 常量而非点名。

---

## 2. Current Stability Risks（当前稳定性风险，按危害排序）

> 每条都给「今天有没有机器证明」。没有的，就是本阶段要补的。

**R1 用户旅程完全无自动化证明（最高）**
发消息/收文件/群同步/重启恢复这些链路，单测覆盖的是**函数**，护栏覆盖的是**文本形状**。
没有任何一层验证过"点一下 → 对面真的出现且只有一条"。
现状证明：`AUTOMATED`（单元级）／`MANUAL-HARDWARE`（旅程级）。缺：§7 全部 8 条旅程。

**R2 CI 从不启动应用**
`verify.yml` 只做静态检查+单测+编译；`build*.yml` 只打包。三个 example（`e2e_peer`/`dual_link`/`mirror_dial`）
需要人手工起实例，且**不在任何门禁里**。⇒ 一次跨进程回归都不会自己响。

**R3（2026-09-26 已收口）26 条不变量里 20 条没有具名钩子**
P01 唯一 msg_id、P02 重复投递安全、P03/P04 outbox 边界、P14 DB 是事实源、P15 event 只是通知、
P19 seq 权威、P20 聊天不被大文件饿死、P21 建链前先验身份等，**只有 prose 的「验证」段或"推荐测试"伪码**。
⇒ 总指令§三列的"物理定律"里，大多数定律当时没有锁。这是"未来 AI 改坏而 CI 不响"的直接入口。

**收口方式不是补 20 个测试，而是先把"绑定"这件事变成机器判据**（补测试而不钉绑定，下次改名照样腐烂）：
`scripts/check-invariant-hooks.mjs`（已进快速层）要求每条 `### INV-Pxx` 有一行 `- 钩子：` 指向
**当场能解析**的对象 —— Rust 用例必须在本平台基线里、`scripts/x.mjs` 必须存在且**已接进门禁**、
`guards:` 片段必须命中 `verify-guards.py` 的真实 Case 名、`e2e:` 片段必须命中多实例 harness 的判据、
`smoke:` 片段必须命中平台 Smoke 清单。写不出名字来就只能写 `NONE —— 理由`，且每轮都点名打印。
逐条绑完的结果：**26 条全部具名 / 0 条登记为无自动化**（P18 当时拿不出判据、先诚实登记为 `NONE`，
同日稍后由 `scripts/check-key-boundary.mjs` 补上真判据，见下）。
⚠️ 两处诚实边界：① 本判据只保证**绑定不失效**，不保证**行为已被穷举** —— P03 里"解密失败不 Ack"
这一子句今天没有专属用例（现有钩子覆盖的是"落库才算 ACK"与"ACK 验归属"），已落到 B-1a；
② 钩子名里的 Rust 用例只认**本平台**基线，跨平台完整性仍由 `check-test-manifest.mjs` 判。

**R4 文档硬数字与代码大面积漂移（本文件实测）**

| 位置 | 写的 | 真的 |
|---|---|---|
| `ARCHITECTURE-MAP.html:1404` | `DB_VERSION = 8` | **9**（`db.rs:20`） |
| `ARCHITECTURE-MAP.html:229/261/298/413/1459` | 133 条 IPC | **129** |
| `ARCHITECTURE-MAP.html:275` | 29 个事件 | EVENTS 表 **28** 行 |
| `ARCHITECTURE-MAP.html:1436` | 181 条护栏 | 185（工作树） |
| `README.md:33/271` | 138 条 IPC / 123 条护栏 | 129 / 185 |
| `docs/AI_ENGINEERING_INDEX.md:14` | 138 条 | 129 |
| `protocol-invariants.md:632` | 「上面 21 节」 | 26 节 |
| `README.md:28`、`AI_ENGINEERING_INDEX.md:64` | INV-P01~P24 | P01~P26 |
| `ARCHITECTURE-EXPLAINED.md:174/381` | 16 张表 | 19 |
| `migration-ledger.md:43` | transport.rs 8836 行 | **10182** |
| `migration-ledger.md:49` | file_relay 86 行 | **504** |
| `migration-ledger.md:57` vs `:107` | 43 个 vs 13 个文件（自相矛盾） | 22 |

⇒ 根因不是"没人记得改"，是**契约图只有 CMDS 第一列被机器核对，其余全靠人诚实**
（`mapContract.test.ts` 头部自己承认）。修法见 §8 阶段 B：**能算出来的数字一律不许手写**。

**R5 `schema.sql` 已经不是事实源**
实测：`src-tauri/src/schema.sql` 14 张表，`db.rs` SCHEMA 18 张（+`content/store.rs` 的 `content_transfers` = 19），
差集 = `favorites` / `group_files` / `group_file_recipients` / `group_recalled_messages`。
没有任何判据读这个文件。⇒ 两条路都合法：要么生成它，要么删它并在文档里声明"唯一真源是 `db.rs::SCHEMA`"。
**当前它是个会误导人的假契约**（§8 B-3 处理）。

**R6 事件 payload 无契约**
`events.test.ts` 双向核对**事件名**，但 28 个事件里只有 3 个（`settings-changed`/`runtime-changed`/`data-cleared`）
有逐字段断言。TS 侧 `listen<T>` 的 `T` 与 Rust serde 结构**零比对**，字段改名/漏字段/类型变化都不红。
ADR-0009（Rust 生成 TS 类型）至今 `Status: Proposed`。

**R7 多实例能力不足以直接支撑双实例 E2E（隔离有洞）**
实测 `state.rs`：隔离了 **DB 文件**（`gosslan-N.db`）、**日志**（`gosslan-N.log`）、**device_id**（`-iN` 后缀）、
**TCP 端口**（59992+N×10）、UDP 59991 共享 SO_REUSEPORT（这正是同机两实例能互相发现的原因，**保留**）。
**没隔离**：`app_data_dir` 本身 ⇒ `downloads`/`cache`/`favorites` 三目录**全实例共用**。
⇒ 双实例收发文件会写进同一个目录，"同名文件/`.part` 已存在"这类用例的判定会被污染。
好消息：接收目录走 `user_dirs::load(&conn, RECEIVE)`，即**每实例 DB 里的设置** ⇒ 不改生产码就能隔离。

**R8（本轮已收口）Windows 那条腿的清单守卫半失效**
成因不是平台差异，是**判据方向不对称留下的洞**：`added`（本平台多跑）只能 warn，
于是一条用例只要**不在 Windows 基线里**，它在 Windows 上消失就不会红 —— 而 mac 那条腿照跑照绿。
Windows 基线曾烂到只有 mac 并集的一部分（487 vs 690）。
收法：`check-test-manifest` 加一条**不依赖 cargo** 的跨平台完整性判据 ——
任一平台基线都不许比"各平台基线并集"少一条说不出理由的用例，
理由只能是源码里现算出来的 `target_os` / `unix` / `windows` 门控（模块声明 + 测试函数头上两类，
解析不出平台约束的 cfg 一律要求"每个平台都得有"）。`--sync-baselines` 按同一套门控把 Windows 基线推到与"源码里应跑的那条腿"一致（条数不在这里抄，
每次加用例都会变；要看真值跑 `node scripts/check-test-manifest.mjs` 让它自己打印），
差额 14 条逐条打印门控出处（`macos_bookmark` / `menu` / `bluetooth_peripheral` 三处模块门控 +
`cache_cleaner` 的 `#[cfg(unix)]`、`user_dirs` 的 `#[cfg(target_os = "macos")]` 两处函数门控）。
⚠️ **诚实边界**：Windows 那份名单是"按门控从 macOS 观测名单推出来的"，不是 Windows 上观测过的。
下一次 Windows CI 就是它的反证 —— 若真少跑了几条，会红并精确点名，届时在 Windows 侧 `--update` 一次即转观测值。
★ **这条反证已落地（2026-09-26 实测，commit `9431d36`）**：`Rust 单测 / 清单（windows-latest）` = `completed / success`
（job `108302557341`；关键那步是第 7 步「跑门禁（rust 组 —— 清单只在 scripts/verify.mjs）」= 真 `cargo test` + 清单核对，
不是只做静态检查）。清单判据是「基线里有、实际没跑 → **FAIL**」⇒ **绿灯本身就证明那份推导名单与 Windows 现实逐条对得上**，
推导值就此转观测值，不需要再在 Windows 侧补 `--update`。
同 commit 其余各腿：macOS 单测/清单、Android 编译门禁、前端、build-macos×2、build-windows×2 全 success，0 failed。
⚠️ 一处**顺带**证出、但不解释机制的事实：「目标目录被删除」那条 L2 用例（`rottled_receive_directory_finishes_failed_never_done`）
也在 Windows 上跑过了 —— 也就是说"目录没了 ⇒ 只剩最后一次 rename 能发现成品无处安放"这个形状**不是 POSIX 独有的**；
至于 Windows 为什么允许带着打开句柄删目录，本轮没有查，不写进结论。

**R9（本轮已收口）一处未提交护栏**
`verify-guards.py` 里 #27 那 4 条（`mobile-layout` 族）已跑非空转验证：四条全部「改坏即 FAIL、恢复即 PASS」，
注入残留 grep 为 0 ⇒ 护栏 181→**185**。教训保留：**未验证的守卫比没有守卫更危险**，
它让人以为那条判据真的被守着。

**R10 两份文档对同一件事说法相反（账要清）**
① `HANDOFF §0` 说 #27 "正在查机制"，CHANGELOG 4.29.45 说两条成因已修；
② `HANDOFF §4-A` 说 "P3 八条下一批从这里开始"，同文档 §2 说批次 i 已逐条复核完；
③ `HANDOFF §13` 说"某群首次开任务窗口仍付一次 WebView 创建、等用户拍板"，而 `736e7c5` 已改成全局固定 `label=tasks`；
④ `review` 文档仍把 P1 标 ⬜，但 `8d5be3a`/`159d14b` 已落地。
⑤ `acceptance/1.0-release.md` 第 21 条"收到未知帧 ⇒ 今天会拆链，属必修"已被 `4dfe93f` 推翻，文档未更新，
且该文件 22 条 P0 **一个 checkbox 都没有**。

---

## 3. Existing Tasks Audit（现有任务逐条重判）

> 来源：`HANDOFF.md`（§0/§3/§4/§9/§12/§13）、`docs/ARCHITECTURE-REVIEW-2026-09-24.md`（P1–P12 + 8 步路线）、
> `docs/acceptance/1.0-release.md`、`docs/adr/0010`、本地任务号 `#NN`（**注意：仓库没有 GitHub issue，
> `#NN` 全是本地任务 id**）、CHANGELOG。共 **26 条真开着的** + 下述新增。
> **不机械接受**：判定列是本轮重新给的，与原计划不一致的地方都写明理由。

### 3.1 A 类——必须修复（进计划）

| id | 一句话 | 判定 | 处置 | 理由 |
|---|---|---|---|---|
| **A-1** | 双实例 E2E harness（启 A/B、等 ready、建友、通信、断言双方 DB+日志+UI、清理、出报告） | **A** | 新建（原任务列表里没有，是总指令§四的核心） | R1/R2 的唯一根治；§7 全部旅程都压在它身上 |
| **A-2** | 故障注入层（断链/杀进程/重启/重复帧/乱序/旧 attempt/错 hash/错 size/`.part` 已存在/DB 锁竞争） | **A** | 新建，`adr/0010` 至今 Proposed ⇒ 从"设计"转"实施" | §八要求；文件链历史上 4 个真机 RCA 全在这一族 |
| **A-3** | 文件传输稳定性实验室（尺寸×故障×恢复的矩阵化永久回归） | **A** | 新建（原 `#25` 判"两段已完成"即收口 ⇒ **改判未完成**） | `#25` 只做到进程内；§七要的是真进程+真磁盘。RCA 记录的那几次 600MB 失败今天仍只能人肉测 |
| **A-4** | `#25-2` 多文件同发 / 大文件+建群并发的自动化回归 | **A** | 保留并并入 A-3 | 曾实测互相拖累（RC4），无回归 = 会再犯 |
| **A-5** | `#25-3` 两端重启后 `.part` 前缀与内存接收器不一致（160MB 根因形状） | **A** | 保留并并入 A-3 | 同上，且"重启恢复"是 DoD 明列项 |
| **A-6** | `#47` Windows 那条腿的清单守卫半失效（"多跑只 WARN"的洞 ⇒ 不在基线里的用例消失了也不红） | **A** | **判据已修**（`209829c` 跨平台完整性判据 + `8c33927` 非空转夹具 + 基线按门控推导）；**残留已清零**（2026-09-26：`9431d36` 上 `Rust 单测 / 清单（windows-latest）` = success，推导名单被真实 Windows 逐条核对通过） | 原判"延后到有 Windows 环境时（技术上只能在 Windows 上 dump）"**已被 `--sync-baselines` 推翻** —— 不需要 Windows 也能把名单推出来并让它可核对。⚠️ ~~观测那一格不在我手里：判据还在待推提交里，push 由用户执行~~ ⇒ **2026-09-26 已成立**：判据与推导名单都已随 `9431d36` 在 `origin/main` 上，并由那次 Windows job 观测过（详见上面 A-6 那段"反证已落地"） |
| ~~A-7~~ | R9：那 4 条未提交用例的注入验证 | — | **已完成**（4/4 改坏即红，残留 0） | —— |
| **A-8** | R5：`schema.sql` 与真源不一致且无人核对 | **A** | 修复（生成或删+声明唯一真源，二选一） | 它是"数据契约"的地基，错了会带错一批结论 |
| **A-9** | **文件 outbox 没有"到点重投"**：`flush_pending_files` 全文**只有 4 个调用点**，且**每一个都在"收到对端发来的某帧"的处理分支里** —— 建链后 `Hello`（`transport.rs:2580`、`:2902`）、**收到**对端 `Heartbeat`（`:3015`）、BLE 侧 `Hello`（`ble.rs:2110`）；**没有任何一处是本地定时器**，`next_attempt_at` 到点无人查看 | **A** | **已拍板（2026-09-26）＝标为人工验证项并登记，不加定时重投**（当前阶段「暂不新增用户功能」，且动的是网络核心重试时序）⇒ **已登记进 §十 Smoke 矩阵**（一行 MANUAL，含人工步骤与"什么算真故障"）；机器侧只判得住"失联期间不许假成功 + 恢复后自愈"那半格 | 2026-09-26 由 `--fault=peer-freeze` 实测发现：对端冻结 30 s 与 60 s，发送侧 `attempts` 一次没涨、`file_transfers` 连行都没有。<br>★ **性质比"缺个定时器"更糟**：触发条件与故障条件**自相重合** —— 对端越是不回话，越是不会有帧进来，也就越不会重试。解冻后 0.5 s 就补齐，恰恰是因为 B 一活就发心跳把 A 带动了（**是"对端证明自己活着"带动的，不是"到点重投"**）。<br>用户可见后果：给"半死不活"（链路没断、也不报写失败）的对端发文件会静静挂着。对照事实：**内容管线是有到点重投的**（`transport.rs:2627-2635` 连卡住的 Active 都重试）⇒ 这是文件腿单独的缺口，不是全局设计选择。 |

| **A-10** | **接收端"写不进去"对用户完全静默**：offer 期建不出临时文件时只发 `FileReject` + 记一条 error 日志，**接收侧不写库、不发事件** ⇒ 接收方不知道有人给自己发过文件，也不知道是自己的磁盘/目录问题（发送方要 43 s 后才看到失败） | **A** | **待用户拍板**（补提示=新增用户可见行为，当前阶段明令禁止） | 2026-09-26 由注入⑤（`--fault=recv-readonly`）照出。触发条件很日常：磁盘满、接收目录设在只读盘/外接盘被拔。现在唯一的痕迹在日志里 |
| **A-12** | **「rename 才算完成」被 `AlreadyHave` 短路绕过 ⇒ 发送侧记 done、接收侧记 failed、文件只活在 `.part` 里，而且再也没人补**：接收端续传时按 **`.part` 现有字节数**回答"我已收到 X"；若那次收尾 `rename` 失败（目录不可写/被换走），字节是齐的但**没有 final 文件**。下一次 offer 收到 `received >= size` ⇒ 发送侧走"接收端已完整收下该文件，直接完成" ⇒ 删队列行 + 台账 `done` | **A**（文件"看起来发过去了"实际不存在 + 双侧终态互相矛盾 + 队列行已删=**永久**没有补救机会） | **已修（2026-09-26，用户拍板候选①）** | 2026-09-26 由注入⑧（`--fault=recv-dir-rotted`：预置**真前缀** `.part` ⇒ offer 不被拒，再把接收目录改只读 ⇒ 只有 rename 塌）实测照出，一轮 13.6 s 内定论：<br>`A 终态 {"status":"gone","aT":"done"} · B=failed · .part=1048576（整份）· final 不存在`。<br>两侧日志互为证据：B 两次报"接收端已有 65536 / 1048576 字节，要求从此续发"，A 第二次收到 1048576 就宣布完成。<br>**与前几格的关系**：⑤（offer 期写不进）只证明"拒绝得干脆"，A-10 是"拒绝时接收方无感"，这一格更糟 —— **两边都给了用户一个结论，而且是两个相反的结论**。<br>★ **病根不是"没补做 rename"，是一个帧承载了两个含义**：`FileReject.received >= size` 在发送端读作"对方已完整收下"并**当场收尾（删队列行）**，而接收端把 `.part` 的**字节数**当续传位置报出去时，说的其实是"我手上有这么多字节"。所以修法打在**唯一那份判据**上：`decide_offer` 多收一个 `size` 输入，**≥ size 的磁盘前缀一律不算进度**（按 0 处理 ⇒ 重新整份收，走真实的哈希校验与真实的 rename），而 `AlreadyHave` 仍然只认台账 `done`。**不补做 rename**：没成品的 `.part` 内容未经校验（注入①早就证明它可以是随机字节），盲改名等于把坏文件当成品交出去；**不加协议字段**：没必要，两个含义分开之后一个帧够用。`from_bytes` 与 `size` 之间的不变关系仍然只有一处定义 —— `lib.rs` 那条"位置判据不许在处理器里再写一遍"的源码守卫照绿。<br>判据（注入⑧ 改口轮，本轮 6 条）：`.part 真被续写 / A 侧终态明确 / attempts 有界 / 接收侧台账不假 done / **交叉自洽蕴含**（发送侧宣布完成 ⇒ 接收侧必须有整份且 sha256 相等的 final 文件）/ **发送侧只能落到 failed**`。⚠️ 最后一条是为倒数第二条存在的：蕴含式在前件永不成立时**永远为真**，那正是 A-9 那一格用打印救回来的同一种空转 —— 加断言前先问"它有没有可能一直绿"。 |
| **A-13** | **「文件传输变慢/停滞」这一档用户可能永远看不到：15 s 提示与 60 s 放弃共用同一个观测点，而对端彻底冻住时那个观测点根本不会被走到** | **B**（少一个用户提示 + 一条自认为存在的放弃出口不可达；状态机仍收敛 —— 拆链 watchdog 先把这一轮结掉，队列照旧重试，不丢数据） | **待用户拍板**（要修得先决定"停滞"到底该按什么形状判：现在的写法要求"链路还开着但对端不推进"） | 2026-09-26 由注入⑨（`--fault=stall-mid`）顺带量出来的。<br>★ **语料级证据（不是"我没看到"，是全量 grep）**：`test-results/` 现存 **156 份 run**（156 个 A 侧 `app.log` + 251 个滚动分片），`[STALL] transfer=` 命中 **0 行**，而同一批语料里 `[FILE] COMPLETED` 命中 **4,230 行** ⇒ 0 不是"日志没抓到"。<br>★ **机制（逐时间线，`run-2026-09-26T05-31-30-478Z`）**：`05:31:36 accept RECEIVED → start streaming` → 冻结 B → **`05:32:21 [FILE] COMPLETED ok=false`（45 s 整）** ⇒ 先到的是**链路 watchdog（15 s × 3）**，不是 `FILE_STALL_ABORT_MS`（60 s）⇒ 在"对端完全冻死"这个形状下 60 s 那一档**结构上不可达**。而 15 s 那一档**只 emit `file-stalled`、不写任何日志** ⇒ "它到底有没有出现过"今天**没有任何手段能回答**，这半条是本格真正的欠账（可诊断性），比"放弃出口不可达"更该记。<br>⚠️ **不许写成"停滞提示坏了"**：`stall_tick` 只在 `send_on_link_with_tick` 因**队列满而原地等**时被调用（`file.rs:1080` 那句注释说的"对端不收时这里就是唯一的观测点"就是它），要证它可达需要造一个"对端还在收字节、但不推进写盘/不回执"的注入 —— 当前 harness 造不出来（`SIGSTOP` 是全冻，不是半快）。<br>**判据形状（修的时候必须先有它）**：本轮这一单的 `COMPLETED` 时刻与链路被拆时刻之差 = watchdog 常数，所以"60 s 那一档在完全冻死下不可达"这条**只能靠可达性用例证明**，不能靠"日志里没出现"当判据（那是同一义反复）。 |
：气泡（`messages.content.size`）与台账（`file_transfers.size`）里的 size 都是**点击那一刻**按磁盘算的，而真正发出去的 size 由投递任务重算（`file.rs:357` `meta.len()`）；`upsert_transfer` 的 ON CONFLICT 只改 status/path/progress、**不改 size**，`fill_message_sha256` 只回填 sha ⇒ 事后没有任何人把 size 更正 | **B**（UI 假状态 + 跨设备显示不一致；但不丢数据、不卡状态机、不影响续传与校验） | **待用户拍板**（改动只有两处写，但落在投递函数里，§十八不许自己扩范围） | 2026-09-26 由注入⑥（`--fault=src-shrunk`）实测照出：入队 1 MB → 实发 4096 B，**A 气泡与 A 台账仍写 1048576、B 台账写 4096**；同一行里 sha256 已经是 4 KB 那份 ⇒ 自己也不自洽。<br>触发条件日常：文件排在离线队列里等对端上线，用户中途在原路径编辑/覆盖了那个文件（剪完的视频、同步盘回写）。<br>修的形状已确定：把 `fill_message_sha256` 扩成「回填真值」（sha + size 一次写）+ `upsert_transfer` 未 done 时一并更新 size；**判据是现成的** —— 注入⑥里那条「实测 size 分歧」打印改成断言（发送侧气泡/台账 size == 落地字节数）就是先红后绿的那条测试 |

### 3.2 B 类——应该优化（能明确降低回归概率才做）

| id | 一句话 | 判定 | 处置 | 理由 |
|---|---|---|---|---|
| **B-1** | 20 条无具名钩子的不变量补钩子（优先 P01/P02/P03/P04/P14/P15/P19/P20/P21） | **B** | **2026-09-26 已收口**（收法与原判不同） | 原判"等 harness 再做行为版"只对了一半：**先补测试会白做**，因为没有任何东西保证"文档指的就是那条测试"——改名/删除照样腐烂。所以先落地 `check-invariant-hooks.mjs` 把**绑定**钉住，再把 26 条逐条绑到**当场能解析**的真实钩子（初版 25 具名 / 1 条 P18 显式登记为无，同日 P18 也补上了真判据 ⇒ 今天 26 条全部具名）。拆掉的两处偷工：① `scripts/x.mjs` 这类钩子额外要求它**已接进门禁**，否则"跑不到的绿"照样算红；② Rust 钩子只认本平台基线。新钩子族见 R3。**剩下的真尾巴记为 B-1a**（见下一行），不再挂"待办 20 条"这种虚账 |
| **B-1a** | 两条只有"半条钩子"的不变量：P18 私钥不跨 UI 边界、P03 的"解密失败不 Ack"子句（现有钩子覆盖落库才算 ACK 与验归属，不覆盖这条） | **A** | **P18 已收口（2026-09-26）/ P03 子句待办** | **P18**：`scripts/check-key-boundary.mjs`（快速层）两条判据 + 5 条夹具自证（含 2 条"不许误伤"反例），判据 A 扫全部 `Serialize` 类型的字段名与密钥类型、判据 B 扫全部 `#[tauri::command]` 体的**出口位置**是否触碰密钥并与注册表逐条对上。⚠️ **诚实边界**：这是"按形状扫"，挡得住"给 `Identity` 补一行 derive"和"命令 `Ok(密钥字节编码)`"，挡不住"先 `let k = sec.to_bytes()` 再 `Ok(k)`"两步搬运 ⇒ 根治要把 `Identity` 的两个密钥字段改私有、只暴露 `sign()`/`dh()`（属重构核心码，另立任务，不算今天已收口的部分）。<br>**P03「解密失败不 Ack」**：仍无任何专属用例，需要一条行为测试（解密失败 ⇒ 不发 Ack 且 `msg_id` 仍可用）。**不许写成"已有测试"** |
| **B-2** | R6：事件 payload 契约（先覆盖有真实消费者的 ~10 个，不做全量 codegen） | **B** | 优化 | ADR-0009 全量生成 TS 类型属大工程，收益待证；先做"逐字段断言"扩面，成本可控 |
| **B-3** | R4：文档硬数字改成**由门禁自己算并写回**，或加"文档数字 == 实算"判据 | **B** | 修复 + 优化 | 教训已记过：写死条数一定腐烂，一律交给执行者打印。**2026-09-25 一夜之内抓到三处同类漂移**：① `docs/acceptance/1.0-release.md` 写「全量 15 步」（实为 16，清单还漏了「db 锁作用域守卫」）→ 已删掉手写清单；② `README.md` 的 IPC 138 → 129；③ **门禁自己的文案也漂**：`scripts/verify.mjs:172` 写「292 个取锁点全扫」，而同一步在同一次运行里实际打印 **284 guard 绑定 + 11 语句临时量 = 295** ⇒ **已做成守卫**（见下）。<br>**机器判据已落地**：`scripts/check-doc-numbers.mjs` 已接进 `npm run verify` 快速层，规则条数**不在这里写**（写了就是这条守卫自己反对的那种数）——
① 文档里凡声明"全量/快速 N 步"，N 必须等于 `verify.mjs --list` **现算**的条数（本会话加了一步，
于是 ① 自动把期望值从原来的两个数抬到新的两个数，**没有一处文档需要跟着改**，这就是"能算的就不许写"的验收形状）；
② 取锁点条数在契约面文件里**一律禁止手写**，真相由该步自己打印；
③ E2E 断言数按 harness 里的模式归堆现算，且每种轮次名**一种都不许消失**（删标签 = 绕过守卫）；
④ **harness 的每条正向注入轮必须被门禁 `--group local` 逐条点名** —— 正向轮清单直接读 harness 的
`const X = FAULT === "…"`，不手抄第二份名单；三种改坏都要红（少点一条 / 名字打错 / 把预期红的 `-lie` 模式塞进门禁）。
④ 是 2026-09-26 补的：§十五把四轮 E2E 接进门禁之后，静默面从"根本没接进来"变成"接进来又被删掉一步"，
而 ①②③ 三条都管不到"这一轮还在不在门禁里"。
**反向自证已跑**：往 `check-lock-scope.mjs` 注一行 `500 处取锁点` ⇒ 守卫退出 1 并点名文件；删掉后 cksum 与注入前一致。
⚠️ 已知边界（守卫文件头也写了）：`CHANGELOG.md` 与本文件是**事故台账**，故意不扫 —— 它俩必须能原样写"当初漂成什么样"。
⇒ 所以本守卫只保证"契约面无第二个事实源"，不等于全仓库无手写数字。IPC 条数那类早已有 `mapContract.test.ts` 实算等值核对。 |
| **B-4** | R7：双实例 receive/cache 目录隔离 | **B** | 修复（作为 A-1 的前置，写进 harness 而非改生产码） | 不隔离会让 A-3 的"同名文件"类用例判定失真 |
| **B-5** | §4-A③ `useAppStore` 4 处直写未收，`storeContract` 禁令只扫 `useChatStore` | **B** | 保留（小步） | 门面唯一性的已知缺口，成本低 |
| **B-6** | §4-A② 群消息卡片跨窗口同步（需向自己窗口 emit） | **B** | **降级为 C**（改判） | 属"新增一条链路"，§「暂不新增功能」；且它是跨窗口一致性问题，应先由 A-1 证明"到底错在哪一步" |
| **B-7** | `review-step-3-1` Persistence 只读连接（`db_read`） | **B** | **保留为待数据决策，现在不做** | §十二明写"先建锁竞争测试，再按真实数据决定" ⇒ 前置是 A-2 的 DB 锁竞争用例 |
| **B-8** | `review-step-3-2` `list_group_files` 1+3N 与 `refreshGroups` N+1 收批量 | **B** | 延后（性能类，无现场故障） | 先有 §7 旅程判据，再谈优化 |
| **B-9** | `review-step-3-4` `export_chat_text` 等待时间上界测试 | **B** | 保留，并入 A-2 | 现在是"无上界可测"，属 harness 能力 |
| **B-10** | §4-C clippy `--all-targets` 约 25 条测试代码告警 | **B** | 延后（口径外） | 门禁是 lib-only；改口径要连同 CI 一起想清楚 |
| **B-11** | `verify:all` 与分层入口（§十五） | **B** | 优化，**排在 A-1/A-2 落地之后** | 现在只有 `verify`/`verify:full`，且 CI 用同一步骤表 ⇒ 一致性已达标；缺的是 `e2e`/`multi-instance`/`fault-injection` 这三层**还不存在**，先造层再造入口 |

### 3.3 C 类——延后（无稳定收益或前置未成立）

| id | 一句话 | 处置 | 理由 |
|---|---|---|---|
| **C-1** | `review-step-7-1` `handle_message` 按域分册 | 延后 | 纯搬家；实测第 7 步主要收益已拿到（冷加载单 IPC、对账三段）。分册本身不减少任何一类故障 |
| **C-2** | `#49`/`review-step-7-4` 拆 `useChatStore`（2168 行） | 延后 | §十一原话"先消除状态串味，再考虑文件大小"。串味由 B-5 与 storeContract 处理，行数不是风险 |
| **C-3** | `review-step-6-2` 次级键选路 | 延后 | 文档自己判"建议先不做，等一条可复现证据" |
| **C-4** | `review-step-6-3` 选路断言（LAN-only 顺序零变化 + 全排列） | 保留为 C | 系于 C-3；等真做选路时再要 |
| **C-5** | `#26` 设备身份一键重生成 + 自定义后缀 | 延后（**功能类**） | §「暂不新增用户功能」；且其子问题"链路上收到与自己相同 device_id 会怎样"应作为 **A-2 的一条断言**先查清，不必先做 UI |
| **C-6** | `#32` 默认头像 / `#35` 原生感 / `#36` Win 内置 WebView2 | 延后 | 全部是新增功能/打包形态，不属稳定版范围。#36 有真实故障面（企业版无 WebView2），但属发布策略不是代码稳定性 |
| **C-7** | §4-A① 未读与投递摘要去重只覆盖 LRU 4 个会话 | 延后 | 原注释自己写"不确定就别动"；A-1 起来后用真实多会话旅程先看有没有现场 |
| **C-8** | `#30` 移动端群已读"不见了" | **保持 AUDITED，不许猜修** | 用户已真机确认"已读在"；根因未定位，缺取证手段 ⇒ 转 §6 Smoke，等一次真机取证 |

### 3.4 D 类——不应该做（本轮明确写死，防"顺手扩大"）

| id | 内容 | 处置 | 理由 |
|---|---|---|---|
| **D-1** | 引入 Actor / task-per-connection 框架；接 `transport/` 那套新栈 | 删除（计划层面） | review「明确不做」原话；新栈那部分死代码已删（`b64c0ad`） |
| **D-2** | 协议词表改造 / 换数据库 / 把 SQLite 换成别的 | 删除 | 无证据、无收益、风险最高 |
| **D-3** | 把"真 socket IO 失败"降级成普通文件错误（忍着复用坏连接） | 删除 | 坏连接必须判死，隔离靠 endpoint 维度 |
| **D-4** | 没有现成信号支撑的"智能选路" | 删除 | 先接线已有拥塞时间戳，且必须先证明 LAN-only 顺序零变化 |
| **D-5** | `#18` A1-L2 第二段回执链 | 删除 | 发送方没有 UI 消费者；已判"不做"，不要再捡 |
| **D-6** | `#25-4` 100MB/600MB 真量级单测 | 删除（改由 A-3 以受控尺寸 + 断点续传语义覆盖） | 单测里跑真 600MB 只买到 CI 时长，不买到判据 |
| **D-7** | 未读改成后端给值 | 删除 | 实测前提不成立（`utils/messages.ts:328` 已判定） |
| **D-8** | `messages(ts)` 索引 | 删除 | `EXPLAIN QUERY PLAN` 实测无收益 |
| **D-9** | `nicknameOf` / `groupReaderIds` 性能改造 | 删除 | 实测 0.066ms/帧，忽略级 |
| **D-10** | F5③ gossip 解密前去重 | 删除（保持现状） | 刻意不改：会造成广播放大 |
| **D-11** | `AI_PROJECT_HANDOFF §8` 的 Noise XX / mDNS / QUIC / 服务端中继 / 账号体系 | 删除（本阶段） | 文档自己写"v1.0 后规划，不要在当前阶段主动实现" |
| **D-12** | ADR-0009 全量"从 Rust 生成 TS 类型" | 延后为 B-2 的窄版本 | 全量 codegen 与"给 ~10 个事件补逐字段断言"不是一回事，后者够用 |

### 3.5 账目矛盾必须先清（不改代码，只改文档）

`HANDOFF §0` 状态表有 11 行仍写"待推/待提"，而版本号已涨到 4.29.45、远端 tip = `aaf238d` ⇒ 表过期。
连同 §3.3 列的 5 处相互矛盾（R10），一并作为 §8 阶段 B 的第一项（AUDITED→DOCS_SYNCED）。

---

## 4. Completed / Invalid / Duplicate / Missing

### 4.1 已完成——**不要重做**（有 sha 或具名测试才列）

0-A1 守卫登记 `69ec7a7`｜0-A2 删三处平行实现 `b64c0ad`｜0-A3 IPC 收缝 + 删 5 条零引用命令
`417ba37`/`69db4b6`/`a021861`/`b1b1cbb`/`3b32f04`/`1c8541c`｜0-B 四条热查询索引 + v8→v9 `87836a4`/`ccdf21e`｜
P1 断链按"该 peer 全链路丢失"门控 + 停滞兜底 `8d5be3a`/`159d14b`｜P2 写失败按错误来源分流 `4dfe93f`/`c56127a`｜
P3 链路队列按字节封顶 `33bf933`｜P4 中继接收改流式 + `chunk_size` 协商 `274d42e`(v4.29.41)｜
P7 四刀（文件终态唯一出口）｜锁内 emit 清零 + INV-P25 判据 `6d8defb`｜第 5 步-1 常驻窗口自愈｜
第 5 步-3 未读过渡竞态｜`#46` `is_fresh` 恒为假（`fresh_schema_alone_has_exactly_the_migrated_shape`）｜
第 7 步-1 切会话冷加载收成一次 IPC（`get_latest_messages`，判据在 `storeContract.test.ts`）｜
`#25` 段 1/段 2 + emit 抑制行为测试 `fe01723`/`f4b3d3f`/`d18db7a`｜F1′ attempt epoch `2ebfe33`｜
F2 大文件不上 BLE(16MiB)｜F4 fsync 出锁｜F5 群同步①②④｜`#40` 桌面全局预览窗 `7f1664d`/`bd93f58`｜
`#41` 任务窗口固定 label + 预热 `736e7c5`｜`#47` 致命类跨平台可拦 `158eda0`/`aaf238d`｜发版脚本同步 Cargo.lock `26f82ee`｜
`#27` 移动端首屏两条成因 `48345bf`（⚠️ 见 §6，真机未证）。

### 4.2 Invalid——**任务本身不成立**（判据被实测推翻，直接删）

- `P3 八条`：逐条复核后 **6 条是幻影**（其中 2 条"未接线"实为已接线但命名不同形状）⇒ 别再照单开做。
- `第 6 步-1` "全链路不健康要返回 None"：前提不成立，已删。
- `acceptance/1.0-release.md` 第 21 条（未知帧会拆链 ⇒ 必修）：已被 P2 修复推翻，**条目本身作废**，文档待更新。
- `review-step-2-3`（2GB offer 不分配全量内存的测试）：P4 已把接收改流式，**这条的形状已被更强的实现取代**；改写成 A-3 的一条断言即可，不单独留任务。

### 4.3 Duplicate——**同一根因被记成多条**（合并）

`§4-A②` ＋ `#30` ＋ 第 5 步系列 ⇒ 都是"跨窗口/常驻窗口状态新鲜度"，归 **J6**。
`#25-2` ＋ `#25-3` ＋ `§4-A P3八条剩余` ＋ `review-step-2-3` ＋ `adr/0010` 的 11 个场景 ⇒ 全归 **A-3 文件实验室** + **A-2 故障注入** 两张矩阵。
`#47` ＋ CHANGELOG 4.29.42/4.29.43 里两条"基线数字不对" ⇒ 同一件"Windows 腿弱"，归 **A-6**。

### 4.4 Missing——**没有任何地方记过，但按 §19 必须有**

1. **M-1 契约漂移检测**（§十三）：现在文档数字漂移是既成事实（R4），但**没有任何判据**读它 ⇒ 需要"文档 == 实算"的门。
2. **M-2 测试报告产物**（§十六）：~~仓库里一个都不存在~~ ⇒ **已落地，2026-09-26 复核并改写**：`test-results/run-<ISO>/{summary.json,summary.html,instance-{A,B}.app.log,instance-{A,B}.stdout.log,after-{A,B}.db,recv/,src/,backup-*/}` 每次跑都生成（本地已积累 60+ 轮目录）。`summary.json` 顶层含 `run/verdict/negative/binary/platform/duration_s/instances/trace/steps/assertions`，`trace` 就是 `{msg_id, transfer_id}`。**仍缺的一格：`screenshots/`** —— harness 走的是"两端 DB + 日志 + 文件"判据，没有任何 UI 驱动，所以截图要么补 WebDriver/CDP 那一层，要么按 §十 明确标成 MANUAL 而不算 PASS（**不许为了填目录名而放一张假图**）。
3. **M-3 Trace 贯穿**：`msg_id`/`transfer_id` 目前散在日志文本里，没有"一条消息跨层可串"的判据。
4. **M-4 数据生命周期 E2E**：旧库→迁移→重启→清空→重初始化，今天只有迁移单测，没有"进程重启后仍正确"。
5. **M-5 平台/硬件 Smoke 清单**：`docs/acceptance/1.0-release.md` 有 22 条 P0 但**零 checkbox、零状态** ⇒ 不是清单，是一份愿望列表。
6. **M-6 `verify:all` 的 e2e/multi-instance/fault-injection 三层**：§十五列的入口里有 **3 层今天不存在**（不是命名问题，是没有被测物）。
7. **M-7 前端事件反向通道的架构判据**：`check-domain-deps` 明确不扫 `src/**` ⇒ 前端依赖方向只有 api 门面一条守得住。

---

## 5. Automation Coverage（自动化覆盖现状）

### 5.1 四层现状

| 层 | 内容 | 数量 | 查得到什么 | **查不到什么** |
|---|---|---|---|---|
| L1 行为单测（前端） | `node --test`，60 个 `.test.ts` | **606** | 纯函数/状态机/合并/解析的正确性 | 组件真实渲染、浏览器布局、跨窗口 |
| L2 行为单测（Rust） | `cargo test --features bluetooth` | **690**(mac)/679(win，按源码门控推出) | 协议/分片/队列/DB/选路/清理的进程内语义 | 真 socket、真进程、真重启 |
| L3 契约对账 | api/events/domain/lock/invariant 等 6 个脚本 + 3 个 test | 20 余条判据 | 名字/归属/注册/取锁形状/双向存在 | 类型与 payload 形状、UI 可达性 |
| L4 护栏非空转 | `verify-guards.py` | 181(提交)/185(树) | "把正确代码改坏必须红" | 反向（无对象时必须非 0）类判据 |
| **L5 跨进程 E2E** | `scripts/e2e-multi-instance.mjs`；入口已接进 `npm run verify:e2e`（= `verify --group local`：**正向轮全部收在这一组**，`verify:all` 串全量层与本地层；反向/lie 模式仍单独跑，因为它们**预期红**）。**另开一层 `npm run verify:release`（= `--group release`，"发版前专项"，2026-09-26 #75）：现在只有三实例链式轮那一步，CI 同样不认领、日常本地层不收**（判据与两条实测边界见 §12.7 末） | **默认轮 16 条断言 / J1+J2**（文本 6 + 文件 8 + 重启 2）；**脏前缀轮 20 断言**（+4：脏 `.part` → 错 hash → 重试补齐）；**续传轮 21 断言**（+5：真前缀必须被续传复用）；**杀进程轮 23 断言**（+7：接收中真 `SIGKILL`，100 MB 在飞窗口）；**冻结轮 22 断言**（+6：对端被 `SIGSTOP` 冻住 ⇒ 失联期间两侧都不许 done、接收目录不许有终名，解冻后自己补齐且只成功一次）；**磁盘轮 22 断言**（+6：接收目录只读写不进去 ⇒ 必须明确失败并止步、重试不许失控）；**改小轮 22 断言**（+6：入队后源文件被截断 ⇒ 只许按磁盘真值收发、两侧终态一致、盘上只留终名那一份）；**多文件轮 22 断言**（+6：三单一起排队、其中两单同名 ⇒ 一张都不许丢、同名不许互相覆盖、落地内容多重集合必须等于源内容多重集合）——这些数由 `check-doc-numbers.mjs` 现算对账；⚠️ 冻结轮**没有**覆盖"失联期间到点重投"（实测失联窗口内发送侧一次都没尝试）⇒ 见 **A-9** | 两个真实进程之间：送达、只有一条、内容正确、outbox 被 Ack 清空、状态过 sending、**文件只有 rename 后落地且 sha256 一致**、两侧终态 `done`、重启后仍正确 | 除 J1/J2/J3 之外的一切旅程；**尺寸阶梯已实测 4 档**（1 KB 极小 / 1 MB 默认 / 10 MB 多片带零头 / 100 MB 在飞窗口，前两档与 10 MB 都收在本地层门禁里，⚠️ 这格原文写"只有 1 MB 与 100 MB 两格实测"是 2026-09-26 按 `verify.mjs` 现算步序改口的过期口径），**未跑的是 §七 列的 10 KB 那一档与 >100 MB**；断链/重复帧/乱序/DB 锁竞争（DB 锁已升 L2，见本节 L2 行）；UI 视觉腿；Windows 腿；routed/BLE/中继三条路径 |

### 5.2 功能区覆盖矩阵（17 区 × 4 类证明）

`●`=真行为断言 `○`=只有文本/形状护栏 `–`=没有 · E2E 列全为 – 即 §7 的欠账

| 功能区 | 单元 | 契约 | 护栏 | E2E |
|---|---|---|---|---|
| 文本消息 | ● | ● | ● | **●** J1（本地 5 连绿 + 反向自证报红） |
| 文件传输 | ● | ● | ● | **●** J2（1 MB 单文件 4 连绿；仅"未断开、单文件、1 MB"这一格）|
| 图片/预览 | ○ | ● | ○ | **–** |
| 群聊/群同步 | ● | – | ● | **–** |
| 任务(群 todo) | ● | ○ | ● | **–** |
| 公告 | ○ | – | ○ | **–** |
| 收藏 | ● | ○ | ○ | **–** |
| 好友/请求 | ● | ○ | ● | **–** |
| 已读/未读 | ● | ● | ● | **–** |
| 通知 | ○ | – | ○ | **–** |
| 搜索 | ● | ○ | – | **–** |
| 共享目录 | ● | ○ | ● | **–** |
| 设置 | ● | ● | ● | **–** |
| 窗口生命周期 | ○ | ○ | ● | **–** |
| 网络通道(LAN/routed/relay/BLE) | ● | ● | ● | **–** |
| 内容补拉 | ● | ○ | ○ | **–** |
| 数据清理/迁移 | ● | ● | ● | **–** |

⇒ **结论很直白：单测与护栏这一层密度不低，E2E 这一层是零。** 所以§四把它定成"整个计划中最重要的一项"是对的。
> 2026-09-26 更新（本表是**审计时快照**，故意不回填）：E2E 列已从 0 补到 **2 / 17**（文本 + 文件传输），
> 其余 15 区仍为 0。补的速度远慢于欠账，所以这张表的重点仍然是"哪些格子还是 –"。

### 5.3 明确不存在（不要以为有）

- 无任何 CI job 启动应用；无任何跨进程通信测试。
- 无 Playwright / WebdriverIO / tauri-driver / wdio 配置，无 `test-results/` 目录。
- 3 个 example（`e2e_peer`/`dual_link`/`mirror_dial`）手工、需要活实例、**不在门禁里**。
- 已有手工多实例脚本：`scripts/run-multi-instance.ps1`（Win）、`t3-presence-relay.sh`、`t2-learn-id.sh`、`e2e-dev.sh`、`t4-mirror-dial.sh` ⇒ **可当 harness 的行为参考，但它们不是测试**（无断言、无报告、无门禁）。
- ADR-0010 故障注入：`Status: Proposed`，代码里只有零散同名用例。
- ADR-0009 类型契约：`Status: Proposed`，未落地。

---

## 6. Manual Coverage（必须承认只能人工的部分）

> 现状问题不是"有手工项"，而是**手工项没有清单、没有状态**（`acceptance/1.0-release.md` 22 条零 checkbox）。
> §8 阶段 A 要把它变成带 `AUTOMATED / SIMULATED / MANUAL-HARDWARE` 标注的 Smoke 矩阵。
> 下面这批**近期只能由用户真机验收**（本环境无屏幕录制/辅助访问权限，起得来窗口但我看不到）：

| 项 | 为什么不能自动 | 标注 |
|---|---|---|
| 移动端首屏（#27）真机首帧布局、转屏 | 需要真机 + 权限弹框时序 | MANUAL-HARDWARE |
| BLE 真实收发（三端） | 无 BLE 硬件、无对端 peripheral | MANUAL-HARDWARE |
| 系统通知点击跳转、桌面角标 | OS 通知中心不可读回 | MANUAL-HARDWARE |
| 沙盒目录书签权限（macOS）/ 厂商后台限制 | 系统与厂商行为 | MANUAL-HARDWARE |
| relay 大文件真链路（含断网/切换） | 依赖外部中继服务器与真网 | 待 A-2/A-3 转 SIMULATED |
| 两台以上真设备联调（acceptance 第 15 条） | 物理设备 | MANUAL-HARDWARE（A-1 落地后同机双实例可转 AUTOMATED 覆盖大部分） |
| 群已读"不见了"(#30) | 根因未定位、缺取证 | MANUAL-HARDWARE |
| **给"半死不活"的对端发文件**（A-9：链路没断、也不报写失败，文件那条腿只在"收到对端一帧"时才动） | 机器侧已由注入④/⑨ 判住"失联期间不许假成功 + 恢复后自愈"，但**"到点自己重投"这个机制今天不存在** ⇒ 没有可判的行为；单台同机也造不出"链路在、对端不回话"的真网络 | **MANUAL**（人工步骤：两台设备，一台发文件、另一台在收到前把网络掐成"连着但极慢"或让它睡眠/断 Wi-Fi 但保留链路，等 ≥2 min ⇒ **看到的应当仍是"传输中"而不是"已送达"**；随后恢复网络，文件必须自己补齐且内容摘要与源一致。若这期间界面被判成"完成"或对端恢复后永远不补 ⇒ 记为真实故障并回来补自动化格子） |

---

## 7. High-Risk User Journeys（高风险旅程 = A-1 harness 的第一批用例）

排序依据：**曾出过真故障 > 状态机最复杂 > 涉及磁盘 > 涉及跨进程 > 其他**。
每条写"必须断言什么"，因为 harness 的价值全在断言清单，不在启动进程。

| # | 旅程 | 为什么最高风险 | 必须断言 | 今天能否自动 |
|---|---|---|---|---|
| **J1** | 文本消息 A→B 全程 | 它是所有语义的地基；RC3 曾出现"接收失败但发送端显示成功" | A 出现 sending→sent；B **恰好一条**、内容正确；`msg_id` 唯一；A 最终 delivered/read；outbox 清空；**重启后仍正确**；无重复通知 | 否 → A-1 后 **AUTOMATED** |
| **J2** | 文件 A→B（小/中/大 × 尺寸阶梯） | 历史 4 个 RCA 全在此；`.part`+rename+hash+attempt 四层状态 | 只有 rename 后 B 目录有文件；进度单调不超 100%；hash 不符必拒且不 rename；`.part` 收完即清；重复 FileDone 幂等 | **部分 AUTOMATED**：1 MB / 单文件 / 不断链这一格已 4 连绿（8 条断言，含 rename 后落地 + sha256 + 两侧终态 `done`）。尺寸阶梯、并发、断链/杀进程仍 → A-2/A-3 |
| **J3** | 传输中断链/杀进程/重启后续传 | 跨链路重试 + Gap 判死曾是 600MB 必死根因 | 断链只影响该端点；旧 attempt 帧必须无效；重启后前缀一致；终态不可被降级（INV-P26） | **部分 AUTOMATED**：`.part 已存在 / 错 hash → 重试补齐`这一格已 2 连绿（4 条断言 + lie 模式反向自证）；**接收中真 `SIGKILL`（100 MB 在飞）→ 重启后按盘上真实字节续完**这一格每轮本地层都真跑（该步 7 条断言；lie 模式 **2/25** 报红 —— ⚠️ 这里原先写的是 `2/23`，是这一格后来又长了判据而没人回来改的账）。★ 2026-09-26 这一格**把自己判红过一次**：期望值取在按下 `SIGKILL` **之前**的 `.part` 快照，而冻结后的真值多出恰好一个 256 KiB 片（快照 4,194,304 vs 重启后的 B 自己读到 4,456,448 并要求从那里续、A 照做）⇒ **红的是判据自己的读数窗口，产品服从的是盘上真值**；期望值已改取"确认已死后冻结"的那一次读数，两个读数都打印出来当探针。**续发点本身是每次实测的函数，不许写常数**（旧那句"实测续发点 786432 字节"就是这种常数，别照抄）；两端优雅重启不二次投递已由 L-B 覆盖。**旧 attempt 帧在 L5 跨进程仍未做**（但 L2 已有 `stale_attempt_frames_are_filtered_but_legacy_frames_never_are`，
`network/file.rs:2983` ⇒ 这里缺的是"过真 socket"那一层，不是"没人证过"）；断链 → A-2 |
| **J4** | 群聊：离线成员→上线后补齐 + gossip 收敛 | RC2「永久不同步而单聊正常」的机制最复杂 | 成员重连后收敛到同一集合；撤回按 G-Set 语义不复活；seq 排序权威；不产生重复应用 | ★ **2026-09-26 拆成两半**：**两实例那一半已 AUTOMATED**（`--round=group`：入队时对端进程还没起 ⇒ 只能靠建链后 `flush_group_outbox` 补发，落库/解密/`GroupAck` 清队列/撤回物化/与 1:1 不串味逐格有判据，反向 `-lie` 恰好 7 条红）；**gossip 多跳收敛那一半也已 AUTOMATED**（同日 #75 `--round=gossip3`，挂在**发版前**那一层 `npm run verify:release`，不进日常本地门禁 —— 判据与两条实测边界见 §12.7 末）。⇒ 这一行原本的"要第三个真实对端"欠账已清，但**实测出一个新的产品缺口**：晚到的群成员拿不到中间人的补推（本格不判它，另立待拍板）。
| **J5** | 首次启动（新库→骨架→首页→身份） | `is_fresh` 曾恒为假；#27 首屏误判桌面；建窗不能阻塞主线程 | 新库只付一次 schema 成本（不打假"正在迁移"）；首帧判移动版；骨架必关；窗口可见 | 部分可 AUTOMATED（DB/日志），视觉仍 MANUAL |
| **J6** | 窗口：隐藏→再聚焦→数据新鲜 | 常驻=不重载；三窗口共用骨架 | 重获焦点必重拉；任务窗换群；预览换内容；无白屏骨架 | 结构部分 AUTOMATED，视觉 MANUAL |
| **J7** | 数据生命周期：退出→重启→迁移→清空→重初始化 | 唯一裁决/终态契约/级联删除的交叉区 | 无幽灵状态；清空后身份仍可用；迁移中途失败可恢复；`failed` 只有一个裁决者 | 否 → A-1 后 AUTOMATED |
| **J8** | 中继：直连失败→relay，且 token 不进日志 | 安全边界 + 唯一"外部服务器"依赖 | 旧版本 offer 明确拒收并给理由；日志/事件里**只有 `token_len`**；中继不改变 hash 语义 | 日志断言 AUTOMATED，链路 SIMULATED |

---

## 8. Recommended Execution Order（执行顺序与退出判据）

> 原则：**先造证明，再动核心**（§二十二 3）。所以顺序不是"先修最险的代码"，
> 而是"先让最险的代码**能被自动证明**"。当前**没有任何 A 类生产代码缺陷是已知的**
> （P1–P7 均已收口），因此阶段 A/B 全是基础设施与账目，不改生产链路——这正好符合§二"先审计不要乱改"。

### 阶段 A — 证明基础设施（最高优先，不改生产逻辑）

1. **A 账**：把 §2/§3.5/§4 的 5 处文档矛盾、`acceptance/1.0-release.md` 的 Smoke 矩阵、
   以及本文件数字一次性对齐 ⇒ 产出带 `AUTOMATED/SIMULATED/MANUAL-HARDWARE` 标注的清单。（**纯文档**）
2. **收口 R9**：4 条未提交护栏跑注入验证，红则留、不红则撤。
3. **A-1 harness v0（里程碑即"能跑通一条"）**：同机双实例启动 → 等 ready（读各自 DB/日志，超时判 FAIL 不退 0）
   → 互加好友 → 等在线 → **跑 J1 全断言** → 清理 → 出 `test-results/run-<ts>/{summary.json,summary.html,instance-*.log,sqlite-*/}`。
   前置 B-4：每实例 `receive_dir`/`cache` 走自己 DB 的设置，物理隔离。
   **退出判据**：连跑 3 次全绿、单次 ≤5 分钟、残留 0 个进程/0 个临时目录、失败时能报出"第几步、预期 vs 实际、msg_id"。
4. **CI 接一层**：`verify.yml` 加 `e2e` job（macOS + Windows 两条腿，§二十优先 Windows x64），**必须调同一套入口**（§十五）。

### 阶段 B — 把"证明"扩到面

5. **A-2 故障注入层**：断链 / SIGKILL / 重启 / 重复帧 / 乱序 / 旧 attempt / 错 hash / 错 size / `.part` 已存在 / DB 锁竞争（§八清单逐条落），
   每条同时是 **ADR-0010 的实施**（顺手把那份 Proposed ADR 关掉）。
6. **A-3 文件实验室**：尺寸阶梯 × 上述故障，覆盖 J2/J3，并把 §4.1 里 F1/F2/F4/F5 的每次历史修复各钉成一条永久回归。
7. **J4/J6/J7**：群收敛、窗口新鲜度、数据生命周期。
8. **M-1 契约漂移门**：文档数字改为实算/写回；`schema.sql`（A-8）一并处理。

### 阶段 C — 把既有证明补强（B 类）

9. ~~**B-1** 20 条不变量补钩子~~ → **已收口（2026-09-26，收法=先钉绑定再补钩子，见 R3）**；~~**B-1a** 的 P18 私钥边界静态判据~~ → **同日已收口**（`check-key-boundary.mjs`，见 B-1a 行的诚实边界）。B-1a 只剩 **P03"解密失败不 Ack"行为用例** 一格。
10. **B-2** 事件 payload 逐字段断言扩面（~10 个有真实消费者的事件）。
11. ~~**A-6** Windows 基线在有 Windows 环境时 `--update` 收口~~ → **判据那一半已作废**（`--sync-baselines` 不需要 Windows 环境就能按门控推名单）。
    剩下的只有一格：**push 之后盯一次 Windows CI**，红了就按它点名的用例在 Windows 侧 `--update` 转观测值。
12. **B-5 / B-9 / B-11** 小步补齐。

### 阶段 D — 由数据决定要不要动 DB / 前端结构

13. **B-7** 只读连接：仅在 §5 的 DB 锁竞争用例给出"确实阻塞 UI"的数字之后才做。
14. **C-1 / C-2** 分册与拆 store：仍延后，除非 A 阶段暴露出"因为文件大才查不出"的具体故障。

### 阶段 E — 稳定版终审（§第五步）

15. 逐项回答 §19 七组验收 + §13 十四个审计维度；只有全部达到标准才宣布完成。
    → **已作答，见 §12**。结论是**不宣布完成**：三组不成立（DB 契约实测不一致、网络缺"非 LAN 出口的跨实例真跑"、
    UI 只到结构级），其中两条等用户拍板（§12.9），一条属环境而非代码。

**每阶段都走§十七小步闭环**：发现→根因→最小修改→测试→代码→局部测试→guard→E2E→文档→完整验证→提交。

---

## 9. 进度状态机（本地图当前快照）

| 任务 | 状态 | 缺什么才能前进 |
|---|---|---|
| 本文件（第一阶段审计） | **DONE**（AUDITED→DOCS_SYNCED 于本轮完成；无生产码） | — |
| A 账 / Smoke 矩阵 | PLANNED | 落文档，不需环境 |
| R9 四条护栏 | **DONE**（UNIT_VERIFIED + GUARDS_LOCKED：4/4 注入验证通过） | — |
| A-1 harness v0 | **DONE**（J1 5 连绿 → 接 J2 后 16 断言 5 连绿 + `--negative` 反向自证报红 + 残留进程 0；设计=§11，结果=§11.7） | 下一步接断链/杀进程与 Windows 腿，再谈进 CI |
| A-2 故障注入 | **E2E_VERIFIED（全部接进 `npm run verify:e2e` 本地层；⚠️ 这一格从此**不写"几格"** —— 总数型声明没有守卫，本轮就抓到它落后了整整两格：⑨ 停滞轮与 ⑩ 发送端被杀轮都已落地补绿，那句"八格"却还留在纸上。逐格清单看 §12.7，条数由 `check-doc-numbers.mjs` 现算）**：脏 `.part`→错 hash→补齐 / 真前缀必须被续传 / 接收中真 `SIGKILL` 后续完 / **发送中真 `SIGKILL` 后重启续完** / 对端失联后解冻自愈 / **字节在飞时冻住对端 ⇒ 盘上进度一字不涨** / 接收目录写不进去 / 入队后源文件被改小 / 连续三单其中两单同名 / 字节收完只塌在最后那次改名。每格都有正向绿 + 只翻一个期望值的 `-lie` 反向红（**断言条数不在这里写**：由 `check-doc-numbers.mjs` 现算并与验收矩阵对账）。ADR-0010 已从 Proposed 落到「注入轮每格都真跑绿」 | §八 剩余：**无**。⚠️ 这一格原本写的是"剩余：三实例并发（两个 offer 都在任一次 rename 之前到达）"，2026-09-26 收口，而**被推翻的正是"要第三个实例"这个前提**（RCA 见本节 A-3 行与注入⑦ 行）。同一句结论当时抄在 A-2 / 注入⑦ 边界 / "覆盖的边界"三处，我第一次只改了 A-3 ⇒ **改复制结论要按处数改全**（本轮由这次自查抓到，不是由守卫抓到 —— 这一族没有守卫）。**尺寸阶梯已按「形状」而不是逐 MB 补进本地层**：`--size=0.001`（1 KB = 单片、零头即整份）与 `--size=10`（10 MB = 多片 + 最后一片是零头）各跑**整趟默认轮**（文本 + 文件 + 重启，16 条断言同源），两档实测全绿、10 MB 档整轮 11.2s ⇒ 10 KB 与 1 KB 同形状（LAN 片长 256 KiB）由 1 KB 档代表，**100 MB 及以上不进本地层**（一次几分钟会拖垮本地与 CI），那一档的大文件窗口由「杀进程轮 100 MB」代偿。⚠️ 曾并列在这条"剩余"里的另两格**已经不成立，别照抄**：**DB 锁竞争**升 L2（见本节「§十二 DB 与锁」行，两条动态判据 + 变异自证）、**连续多文件**由注入⑤覆盖（只覆盖"同 peer 串行 flush"这一时序）。**目标目录变化**按形状拆两半各自已做：目录仍在但不可写（`EACCES`）＝L5 注入⑧两个真实进程；整个目录被删走（接收句柄挂在被 unlink 的 inode 上、写入与 fsync 照旧成功 ⇒ **只剩最后一次 rename 能发现成品无处安放**）＝L2 `network/file.rs::rotted_receive_directory_finishes_failed_never_done`（已用"把 rename 失败吞掉就算成功"变异自证会报红）。⚠️ 重复帧·乱序帧·旧 attempt 帧·重复 FileDone 这四格**不在"未做"里**：L2 已有具名证据（见 §11 harness 边界行的分层清单），L5 帧级伪造判为**会造出假绿 ⇒ 不可自动**；「单链路断开」同样单机无 root 不可自动（伪造被 route 优先级绕过）|
| **A-12** `.part` 字节数冒充「已收完」 | **DONE（2026-09-26）**：`580cff0` 按候选①改码 + 单测先红后绿 + 注入⑧两侧一致（`A=failed / B=failed`）+ 交叉自洽断言加回 + 不变量 §17 第三条挂上钩子 + `verify:full` 19 步全绿 + **同一入口在 CI 上再绿一次**（`9431d36`：macOS 与 Windows 两条 Rust 腿、前端、Android、三端出包全 success，0 failed） | 边界另记：目录恢复可写之后这一单**不会**自己补完（协议里没有那条码），不算这一格欠账；它在 A-10/A-11 那一族"失败后无人重驱"里，要修就得先拍板 |
| **§十二 DB 与锁** | **UNIT_VERIFIED（2026-09-26）**：这条耦合热点**第一次有动态证据** —— `db/clocks.rs` 两条（8 线程 × 12 条压生产写路径，判 seq 连续不重号 / `msg_id` 唯一 / 一条不丢；500 行长事务与 4 个普通写入并行，判正确性 + 一条**故意给宽**的 5s 上限）。非空转：往锁内塞 `sleep(6s)` ⇒ 恰好一条报红（`锁内不许出现秒级慢活：6.025s`），撤掉即绿 | ⚠️ 跑在**内存库**上 ⇒ 只证"一把锁 + 生产函数并发不坏"这个形状，不是磁盘 fsync 的量级；⚠️ `DB busy test` **判为结构性不存在**（全进程一条 `Connection` + 一把 `Mutex`，`db.rs` 里没有 `busy_timeout`，因为没有第二个连接去抢表锁）⇒ 做出来是一条永远不成立的判据，不补；⇒ "缩短锁生命周期 / `try_lock` / 后台任务隔离"今天**仍没有被测出来的必要性** |
| A-3 文件实验室 | **AUDITED（⚠️ 这行原来写 `DISCOVERED`，是状态机没跟上实测的漂移 —— 2026-09-26 按现状改）**：这张矩阵实际已由 A-1/A-2 交付掉大部分 —— 尺寸阶梯按形状进了本地层（1 KB 单片 / 10 MB 多片带零头，各跑整趟默认轮）、100 MB 由杀进程轮代偿、接收/发送两侧真 `SIGKILL`、冻结/停滞/磁盘/改小/改口/多文件/改名塌在最后各有一轮跨实例判据（逐格看 §12.7，条数由 `check-doc-numbers.mjs` 现算）。**真正还缺的只有一格已被收口（2026-09-26）**：三实例并发（两个 offer 都在任一次 rename 之前到达）—— 拍板＝改名共存后按 RCA 落地，**根因不是"缺第三个实例"**：单聊的 `final_path` 在 begin 就用 `unique_path` 定死，两份同名同时在途 ⇒ 两者拿到同一个名字 ⇒ 后一次 `rename` 在 POSIX 上直接覆盖前一份（两行台账都 `done`、一个气泡指着已不存在的字节）。修法＝在单聊收尾真正落地前再确认一次名字（与群聊收尾同形状，那边早就有这半格）。判据 `network/file.rs::two_same_named_transfers_do_not_overwrite_each_other` 直接驱动生产 `finish_receiver_into`（先跑红：两行报同一个路径；修后绿，同模块 66 条无回归）。另有一条已登记的边界：A-13（停滞两档在"对端完全冻死"下都够不到、15 s 那一档连日志都不写）| A-1 + A-2 |
| A-8 `schema.sql` | AUDITED（**取证完成 2026-09-26**：本轮实测两边**表名集合不等**，见 §12.8；它同时是 `check-change-budget` 的敏感文件与 `domains.data.mjs` 的域清单成员 ⇒ 退役要一起改这两处名单） | 缺**一句拍板**（§12.9 的 A「自动生成」/ B「退役」，推荐 B）。不选就不动：**留着一份自称一致其实不一致的 DDL，比没有更坏** |
| A-6 Windows 那条腿 | **DONE（2026-09-26）**：跨平台完整性判据 + 非空转夹具 + Windows 名单按门控推导 ⇒ **不再需要"等有 Windows 环境"**；★ 残留那一格已由 `9431d36` 的 `Rust 单测 / 清单（windows-latest）` = success 兑现（清单判据「基线里有、实际没跑 = FAIL」⇒ 绿灯即逐条核对通过，推导值转观测值） | 观测方式记录：job `108302557341` 第 7 步 = `node scripts/verify.mjs --group rust`，**与本地同一入口**（§十五要求）；今后再加平台无关用例，收尾仍是 `--sync-baselines`，不用等环境 |
| B-1 不变量补钩子 | **GUARDS_LOCKED（2026-09-26）**：绑定本身已成判据（`check-invariant-hooks.mjs` 在快速层）+ 该判据的非空转夹具已登记（改坏即 FAIL、恢复即 PASS） | 26 条全部具名（0 条 NONE），尾巴拆成 B-1a 两格 |
| **B-1a-P18** 私钥不跨 UI 边界 | **GUARDS_LOCKED（2026-09-26）**：`check-key-boundary.mjs` 两条判据在快速层 + 非空转夹具 1 条（给 `Identity` 补一行 derive ⇒ FAIL，撤掉 ⇒ PASS） | ⚠️ 残留：两步搬运扫不住，根治要密钥字段私有化（属重构核心码，另立） |
| **B-1a-P03** 解密失败不 Ack | **AUDITED（2026-09-26，缺口已按现物重读并收窄，见 §12.4 末）**：落库那一半有 3 条具名判据，缺的只有"不许发出 Ack"这半边 | 延后（不是缺夹具：`AppState` 持具体 Wry `AppHandle` ⇒ 补这条要先决定"愿不愿意为可测性动最热路径"，与 C-1/C-2 同批，见 §12.4 末） |
| C-1/C-2 分册/拆 store | 延后 | 需要 A 阶段暴露具体故障 |
| D 类 12 项 | 关闭 | 不再重开，除非有新证据 |

---

## 10. 复算方式（每个数字怎么来的——下一个人不必猜）

```bash
# 注册命令数
node -e 'const s=require("fs").readFileSync("src-tauri/src/lib.rs","utf8");
 const m=s.match(/generate_handler!\s*\[([\s\S]*?)\n        \]/);
 console.log([...m[1].matchAll(/commands::([a-z0-9_]+)/g)].length)'
# 前端用例数
node --test --experimental-strip-types --test-reporter=tap <package.json scripts.test 里那批文件> | grep -c "^ok "
# Rust 用例数
wc -l src-tauri/test-baseline.macos.txt src-tauri/test-baseline.windows.txt
# 护栏用例数 / 不变量数
grep -c "^    Case(" scripts/verify-guards.py
grep -c "^### INV-P" docs/protocol-invariants.md
# schema 差集（本文件 R5 的来源）
grep -oE "CREATE TABLE IF NOT EXISTS ([a-z_]+)" src-tauri/src/schema.sql | awk '{print $NF}' | sort
grep -oE "CREATE TABLE IF NOT EXISTS ([a-z_]+)" src-tauri/src/db.rs       | awk '{print $NF}' | sort
# 真源版本
grep -n "pub const DB_VERSION" src-tauri/src/db.rs
```

**纪律（本轮又验证了一次）**：文档里任何"N 条/N 张/N 行"都不许手抄；
子 agent 报的"实测值"必须自己复跑一遍才算数（本轮 L2 数字 `651 vs 基线 690` 的差异，
就是"数函数名"与"数 harness 清单"两种口径 —— 取后者，因为门禁用的是后者）。

---

## 11. 附录 A：双实例 E2E 基础设施设计（第二阶段产物）

> 判据先说结论：**不改一行生产码就能跑到 J1/J2/J3 的全链路断言。**
> 唯一被排除的是 UI 视觉腿（下表 L-D），那部分诚实留在 §6 人工清单里，不冒充 PASS。

### 11.1 现有能力实测：可驱动的入口只有两个

`env::var("GOSSLAN…")` 全仓只命中两处：`lib.rs:328`（`GOSSLAN_AUTOSTART=1` ⇒ 强制 `0.0.0.0` 起网络）
与 `state.rs:1829`（`GOSSLAN_INSTANCE`）。**没有**控制套接字、没有 CLI 驱动面、`tauri` 也没开 `test` feature。
⇒ 任何"让 harness 能发消息"的方案要么加一条新的可驱动面（=新增生产代码 + 新增安全面，本阶段禁止），
要么走下面这条路。

### 11.2 驱动层：四档，按「要不要动生产码」排序

| 档 | 手段 | 覆盖什么 | 生产码 |
|---|---|---|---|
| **L-A 预置状态** | 实例**停机时**写它的 SQLite，把"用户点了发送"变成"已入队的事实" | 发消息（`outbox`+`messages` 双行）、发文件（`file_outbox`+磁盘真源文件）、清空数据、旧库迁移（预置 `user_version`） | **零** |
| **L-B 进程级操纵** | `SIGKILL`/`SIGTERM`/重启、TCP 探测、日志与磁盘断言 | 断链、杀进程、重启恢复、崩溃后 `sending→pending` | **零** |
| **L-C 协议级注入** | 复用 `examples/e2e_peer.rs` 已实现的 17 种消息面，harness 扮演第三个真实对端 | ~~重复帧、乱序 Chunk、错 hash、错 size、旧 attempt、重复 FileDone~~ ⇒ **2026-09-26 重判：这六格早已有 L2 点名证据，再造一遍是重复覆盖**（`file_relay.rs:337` 重复 Chunk 不重复计数、`network/file.rs:2983` 旧 attempt 帧被过滤而 legacy 帧永不、`file.rs:3819` 完成的 transfer 拒重复 offer、`lib.rs:3210` 重复 FileDone 仍回 Ack、`ble_framing.rs:461/470` 乱序仍可完工 / 重复与残缺不许完工、`db/favorites_tests.rs:1675` 重复 transfer_id offer 幂等）。<br>⚠️ **这句里「错 size」那一格当时是被顺手算进去的**（上面六条锚点没有一条判 size）：本轮实测只有 `finish_receiver_into` 的"字节数与声明不符 ⇒ 不算成功"（那格 ②），而**写盘之前的 chunk 级上限** —— `plaintext_len > size - received` —— 一直内联在吃 `&AppState` 的 `write_chunk` 里，从来没有行为测试。现已按仓内先例抽成纯函数 `chunk_exceeds_declared` 并补 `chunk_length_may_fill_declared_size_but_must_not_exceed_it`（四条边界：恰好填满=放行 / 单片即整份=放行 / 超一字节=拒 / 已收满再来片=拒 / size=0 不许写字）。<br>★ **接线为什么不再加一条文本守卫**：`chunk_exceeds_declared` 是 `pub(crate)`，删掉 `write_chunk` 里那一行之后，不带 `--test` 的 `cargo check` 就打 `warning: function ... is never used`（实测），而 CI 的 clippy 是 `-D warnings` ⇒ **调用点存在性由编译器兜住**，符合 §十四"不要无意义增加 guard 数量"。变异也各自跑过：把 `>` 写成 `>=` ⇒ 新用例在"最后一片正好补到 size 必须放行"那句红（这条就是防「过度加固」的）。<br>★ **同一天补掉最后半格**：零字节文件走**单聊**收尾原先只有群路径有
`group_done_empty_file_creates_zero_byte_file`，1:1 的 `finish_receiver_into` 没有 ⇒ 新增
`empty_file_finishes_done_on_the_one_to_one_path_too`（正：done + 真实 0 字节成品 + 路径可读 + 不留 `.part`；
反：**`size = 0` 不是免检通行证**，摘要不符必须 failed 且不许改名 —— 反面才是让这条用例能咬人的那一半，
否则最像"合理优化"的回归（"空文件哪来的哈希可比"）会静默通过）。⚠️ 诚实口径：这组用例**第一次就绿**，
它证明的是"今天的行为正确"，不是"修了一个缺陷"；§七里当时剩下的那一格（“两个 offer 都在改名之前到达”）已于 2026-09-26 收口，而**被推翻的正是“要三实例”这个前提**：它是单聊收尾的定名竞态，L2 就能钉（`network/file.rs::two_same_named_transfers_do_not_overwrite_each_other`，先红后绿）。⚠️ 这句“只剩 X”在此之前已被实测推翻过一次：当时「发送过程中杀进程 × 重启后继续」也没有判据，注入③ 杀的是接收端 —— 补成注入⑩ 之后才重新成立。L-C 剩下的增量只有“两个发送者→一个接收者”的**线上真并发**与断链（撞名那一格已从这块依赖里摘掉 ⇒ 它不再需要三实例）⇒ **延后** | 延后（断链那半要 root、真并发那半要第三个实例；**撞名那半已不需要**） |
| **L-D UI 驱动** | WebDriver / `tauri-driver` | 真实点击与像素 | 零，但 macOS 需 Appium、CI 跑不动 ⇒ **本轮不做** |

L-A 之所以成立，靠一条已核对的实现细节：`transport.rs:4321` `reseal_for_send` 只有在
`content` 以 `enc1:` 开头时才去 `messages` 行取**明文**并用 `friends.x25519_pubkey` **重新封袋**
（`:4353` `reseal_chat_content`）。⇒ harness 写 `"enc1:harness"` 占位 + 一行明文 `messages`，
真实密钥由应用自己完成加密，**测试里不需要任何密码学实现**，也就不存在"测试自己实现了一套加密"的假通过。
这正是 enqueue-before-deliver 不变量的红利：入队即事实源。

### 11.3 隔离方案（B-4 的落地形状）

| 维度 | 怎么隔离 | 证据 |
|---|---|---|
| DB / 日志 / device_id / TCP | `GOSSLAN_INSTANCE=N` ⇒ `gosslan-N.db`、`gosslan-N.log`、`<base>-iN`、`59992+N*10` | `state.rs:1186/1207/1271/1281` |
| 接收目录 | 停机预置 `settings.downloads_dir` = `test-results/run-<ts>/recv/<n>/`；**绝不预置 `downloads_dir_bookmark`**（macOS 上 `load()` 优先信书签，脏书签会盖掉路径） | `user_dirs.rs:37/49-59`，`state.rs:1233` |
| **互联通路** | 我原先记的「同机 LAN 天然失效」**过头了**：两实例同机实测**能**靠 announce 建链（日志 `+conn peer=… path=Lan`）。`SO_REUSEPORT` 的分担在 ≥3 实例 / presence 学习场景才致命（`t3-presence-relay.sh:16-18` 实测的是那个形状）。仍预置 `settings.routed_endpoints`，但它是**确定性兜底**、不是唯一通路 | 本轮实跑 + `discovery.rs:198-205` |
| 用户真实库 | 只隔离**文件名**不隔离目录 ⇒ 每次 run 先把现有 `gosslan-N.db` 移进 run 目录备份，退出时还原 | `state.rs:1174` |

### 11.4 预置 ≠ 用户操作：13 个陷阱，每个都写成断言而不是注释

1. `settings.device_id` 在 `instance>0` 时**不是**运行时 id ⇒ 所有外键必须用 `-iN` 后缀形式。
2. `settings.tcp_port` 对 `instance>0` 是死值（端口由公式来）。
3. 同机 LAN 发现不可用 ⇒ 必须 routed_endpoints（见上）。
4. `x25519_secret`/`ed25519_secret` 格式错 ⇒ 应用**静默重新生成**而不是报错 ⇒ 预置后必须回读校验。
5. `update_friend_pubkeys` 是 `COALESCE` 只填空、先到先得 ⇒ 预置错 `ed25519_pubkey` 会永久拒 Hello。
   **对策**：`x25519_pubkey` 必须预置真值（否则 re-seal 拿不到密钥、明文发出被对端静默丢），
   `ed25519_pubkey` 一律留 NULL 交给 TOFU 自绑。公钥由 harness 从 A 自己 DB 里的 secret 推导
   （PKCS8 定长前缀 + `createPrivateKey` → `export({format:"jwk"})`，Node 原生支持，已实测）。
6. `set_message_status` 带 `status NOT IN ('read','delivered','recalled')` 单调闸 ⇒ 预置终态会把状态冻住。
7. `conversation_clocks` 每次 `db::init` 从 `MAX(messages.seq)` 重同步 ⇒ 手写 seq 会被抬高。
8. `file_outbox.local_path` 必须真实存在，否则每次重试白烧一个 `attempts` 配额。
9. WAL + `synchronous=NORMAL` ⇒ 只在进程停着时写库，写完 `wal_checkpoint(TRUNCATE)`，遇 `locked|busy` 重试。
10. **`outbox` 与 `messages` 必须成对插**（同一个事务的形状）：只插 outbox ⇒ re-seal 没有明文；
    只插 messages ⇒ Ack 那句 `SELECT sender_id FROM messages` 找不到人，状态永不前进。
11. `nickname` 缺失时会被自动生成覆盖。
12. 可达对端的 outbox 有 **120s** 判死窗口 ⇒ 断言必须在此之前完成（不可达对端才持 7 天）。
13. **harness 读的那份日志不是静态文件**：应用每次 append 前量一次大小，超 512 KB（= 524288 B）
    就把整份 `x.log` **改名**成 `x.old.log` 并**覆盖上一份旧档**，当前档从零写（`logging.rs:296-324`）。
    日志跨启动**追加**、harness 历史上从不清档 ⇒ 攒到临界点后，"等 boot 完成行""这条 transfer_id
    出现过"这类判据会在轮转瞬间看不见那一行，而**旧档里更早的内容会被删掉**——所以"跨两份文件数次数、
    和启动前的次数比"这种修法仍然会红（计数会**变小**）。两次都实测过，见 §12.7 末尾"判据层自己的账"。
    **对策**：每次 launch 前把该实例的 `.log`/`.old.log` **整份移进 run 目录**（`stashLogs`，移动不删），
    本轮写的行只能在当前档里；判据层单独成文件 `scripts/e2e-logtail.mjs`，自带 5 格夹具自证。

### 11.5 生命周期与产物

```text
备份用户 gosslan-N.db → 起 A/B 一次（只为生成身份密钥；每次 launch 前先把该实例的历史日志移进 run 目录）
→ 读各自 secret、推导公钥 → 预置 friends（x25519 真值 / ed25519 NULL）
→ 预置 routed_endpoints + downloads_dir → 预置本轮动作（L-A）
→ 起 A/B → 就绪探针（TCP 可连 + 日志出现 boot 完成行）→ 等链路（日志 +conn peer=）
→ 断言（双方 DB / 日志 / 磁盘文件）→ SIGTERM → 残留进程数必须为 0
→ 写 test-results/run-<ts>/{summary.json,summary.html,instance-*.log,sqlite-*/,recv/}
→ 还原用户 DB
```

判据（不满足就不算 harness v0 完成）：连跑 3 次全绿；单轮 ≤5 分钟；失败时报出
「第几步 / 预期 vs 实际 / `msg_id`」；残留进程 0；**永不触碰用户的 `gosslan.db`**（只碰 `-N`）。

### 11.6 明确不做（§18）

- **不加**任何控制套接字 / 新命令 / 新 feature-gated 驱动面。要等 L-A~L-C 证明"确实有一条旅程无法用预置表达"时，
  再单独提这项并给证据。
- **不接** WebDriver/tauri-driver：macOS 侧要 Appium，CI 不稳定，收益不如把 L-A~L-C 做扎实；UI 腿留在 §6 人工清单。
- 不在 v0 就把 E2E 塞进 CI：先本地连跑 3 次全绿，再谈加 job（§二十优先 Windows x64）。

### 11.7 v0 实跑结果（2026-09-25，macOS arm64，release 二进制）

| 判据 | 结果 |
|---|---|
| 正向 | J1 阶段 **连跑 5 轮全绿**（每轮 8 条断言）；接 J2 + 重启注入后 **16 条断言 / 5 连绿**（第 5 次是接上故障注入后复跑，确认没弄坏原路径）。最新一轮 `/usr/bin/time` 墙钟 **12.8 s**（步骤计时合计 ≈8 s，报告里的 `duration_s` 就是这个合计、不含进程起停）。⚠️ 原先手写的「单轮 22–25 s」本轮**没能复现**，量纲也不同 ⇒ 已按实测改写；这类数字**每次要重新跑，不许抄上一版** |
| 故障注入正向 | `--fault=poison-part`（= `npm run test:fault-injection`）**20 条断言 / 2 连绿**。注入的是「B 侧预先存在 4096 字节随机内容的 `<tid>.part` + A 侧一条 pending 的 1 MB outbox」。**A 日志实测到的完整链条**：`offer(from=0)` → B「接收端已有 4096 字节，要求发送端从此续发」→ A「从断点续发」→ 第 1 次 attempt `COMPLETED ok=false`（脏前缀被播种进 hasher ⇒ 整体 SHA-256 必不符）→ 10 s 后 outbox 重试 → 第 2 次 attempt 前缀已丢弃、整份重收 → `ok=true`，终名文件 1048576 B、sha256 与源一致、两侧 `done`、`outbox=0`、无 `.part` 残留 ⇒ **「错 hash」这一格第一次被真进程跑过，而且是在没有一条生产码改动的前提下** |
| 故障注入③正向 | `--fault=kill-mid`（= `npm run test:fault-injection:kill`）**23 条断言 / 1 连绿**。关键改法：**这一单由判据自己入队** —— 前两版按前两格的惯性在停机时预置，于是 100 MB 在「起 A/B 等链路」那一步里就传完了，判据去看时 `.part` 早没了（连红两轮，红的全是我的时序，不是产品）。改成入队后用 50 ms 自旋盯 `.part`（`waitFor` 是 500 ms 粒度，抓不住 0.78 s 的窗口）。实测链条：`.part` 涨到 **786432 字节**（768 KiB）⇒ `SIGKILL` B ⇒ 那一刻**终名文件不存在**且 **A 不是 `done`** ⇒ 重启 B ⇒ A 日志「接收端已有 **786432** 字节」⇒ 拼完的文件 sha256 与源一致、两侧 `done`、`outbox=0`、无 `.part` 残留 ⇒ §七「接收过程中杀进程」+ §八「对端进程退出」第一次被真进程跑过，仍零生产码改动。<br>⚠️ **这一行是"首跑当时"的快照，两处数已经过期**：那一轮 23 条 / 续发点 786,432 —— 今天这一轮的**条数由顶部「轮次账」现算**，而**续发点每次实测都不同**（同一天里量到过 4,456,448 / 6,815,744 / 7,340,032），并且它的期望值口径 2026-09-26 改过一次（改取"确认已死后冻结"的那次读数，判据为什么把自己判红过见§十四那条 ★）。**别把这一行当现状抄**。<br>顺量到的尺寸事实（§七阶梯第一格）：100 MB 单文件回环 **~0.78 s** 传完（≈130 MB/s，`.part` 每 ~52 ms 涨 6.5 MB）；默认轮在 `E2E_FILE_MB=100` 下 16 条断言同样全绿。<br>
| 反向自证 | 三个模式各一次，都是**跑出来的**，不是推的：<br>① `--negative`：两端照常起、链路照建，只把这条消息的收件人换成幽灵 id ⇒ 投递断言报红、退出码 1。链路是好的，所以红只能来自「没送到」，不来自「B 没启动」。<br>② `--fault=poison-part-lie`（= `npm run test:fault-injection:selfproof`）：注入完全一样，只把判据比对用的期望摘要换成 64 个 0 ⇒ **恰好 2/20 报红**，且红的理由是「预期 000000000000… / 实际 81972590575b…」「预期 双侧非 done / 实际 B=done A=done outbox=0」⇒ 这四条断言读的是**真文件字节与真 DB 行**，不是同义反复；另外两条按互斥蕴含正确保持绿。 |
| 清理 | 残留 `pgrep` = **0**；用户原有 `gosslan-1/2.db` 每轮自动备份、结束时还原 |
| 产物 | `test-results/run-<ts>/{summary.json,summary.html,instance-{A,B}.app.log,instance-*.stdout.log,sqlite-{A,B}/,recv/,after-*.db,backup-*/}`<br>★ **§十六 的"失败时能沿链追踪"已有实测产物（2026-09-26 `run-2026-09-26T01-42-36-986Z`）**：跑 `npm run test:fault-injection:rename-selfproof`（`--fault=recv-dir-rotted-lie`）⇒ 退出 1、**恰好 1 步 FAIL**，那条 step 记录里同时带 ① 断言的 `expect:"done" / actual:"failed"`（反向自证翻的就是这个期望值），② `logs: {A: [...], B: [...]}` —— 由 `traceExcerpt()` 按本轮 `msg_id`/`transfer_id` 从两端应用日志里摘出的命中行（本轮 A 4 行 / B 3 行，末行分别是 `[FILE] COMPLETED tid=…` 与 `[ACK] 重复消息仍回执 msg_id=…`）。⇒ 顺着 summary.json 就能从"哪条断言红"走到"对端当时说过什么"，不需要人去翻 500 KB 日志。<br>⚠️ 一条如实记录、**不当成缺陷**的观察：A 侧那行 `COMPLETED ok=true` 与台账终态 `failed` 不同名（前者是这一轮投递尝试跑完，后者才是唯一裁决）—— 读日志时容易误判成 A-12 复发。要不要把措辞改成 `ATTEMPT-FINISHED` 属可诊断性问题，未拍板前不动生产码。<br>⚠️ 跑之前的固定前置：改过任何 Rust 源码后 harness 会以 **exit 2** 拒绝运行（"二进制比源码旧"），必须先 `cargo build --release`（本轮实测 1m46s）。这道闸是故意的 —— 否则测的是旧代码。 |
| 覆盖 | **J1（文本 A→B + 重启后仍正确）+ J2（1 MB 文件 A→B）+ J3 的第一格（脏 `.part` 前缀 → 错 hash → 重试补齐）= 默认轮 16 断言 5 连绿 / 故障轮 20 断言 2 连绿**。J2 钉的是：只有 rename 后的 `<transfer_id>.bin` 才算落地、字节数 1 MB、sha256 与源一致、A 侧 `file_outbox` 收尾删除、两侧终态 `done`、同一条传输只记一次、接收目录无 `.part`/改名残留 |
| 注入②（续传轮）| `--fault=resume-prefix` = `npm run test:fault-injection:resume`：B 侧预置**源文件自己的前 64 KiB** 当 `.part`。**21 断言 1 连绿**，A 日志实测「接收端已有 65536 字节，从断点续发」⇒ 对**有效前缀**确实走续传而不是从 0 重灌，且拼出来的 sha256 == 源、两侧 done、outbox=0、只剩 1 个终名文件。`--fault=resume-prefix-lie`（期望字节数减半 + 期望摘要换 0）⇒ **恰好 2/21 报红**，红字带着 `预期 32768 / 实际 65536` 与 `实际 877f591f4162…` ⇒ 这两条也不是空转。 |
| 注入④（冻结轮）| `--fault=peer-freeze` = `npm run test:fault-injection:freeze`：**SIGSTOP 冻住 B**（进程活着、TCP 栈照样收 SYN、链路在 A 侧仍是"open"），冻结期内把 1 MB 单入队，等 `FREEZE_MS`（默认 30 s，<45 s ⇒ 不触发 watchdog 拆链），期间读两侧 DB，再 SIGCONT。**22 断言 / 1 连绿**（判据改写后又复跑一次仍绿；冻结时长 30 s 与 60 s 都实测过），实测：失联期间 A 侧 `file_transfers` **无行**、`file_outbox` 仍 1 条 pending、B 侧无行、无 `.part`、无终名 ⇒ 三条"不许假成功"全绿；解冻 → 终名落地 **0.503 s**、sha 与源一致、两侧 done、outbox=0、只留 1 个终名文件。<br>`--fault=peer-freeze-lie`（只把期望摘要换成 64 个 0）⇒ **恰好 1/22 报红**、退出码 1 ⇒ 那 6 条判据读的是真文件字节。<br>★ **这一格真正的产出是它证伪了自己的设计**：原计划钉"write 成功 ≠ 已送达"，但实测 `attempts` 一次没涨 ⇒ **A 在失联期间根本没有尝试**，"在飞的写"不存在。根因＝outbox 只由链路事件驱动 flush、没有到点定时器（→ **A-9**）。所以这轮只声明它证明了的东西：失联期间不许假成功 + 恢复后自愈；**不**声明覆盖"到点重投"。 |
| 注入⑤（磁盘轮）| `--fault=recv-readonly` = `npm run test:fault-injection:disk`：把 B 的**接收目录整体 chmod 0500** 再入队 1 MB。B 活着、照常发心跳 ⇒ 这一格真的会走满重试链。**22 断言 / 1 连绿**，实测：入队 → **43.2 s** 后队列行落到 `{"status":"failed","attempts":5}`；A 日志 `[FAILED] reason=连续重试超限（链路长时间未恢复）`；B 日志三次 `接收文件初始化失败: Permission denied (os error 13)`。<br>六条判据：注入真生效（B 日志有初始化失败）**+ 重试真发生过（attempts≥2 —— 与冻结轮互为对照）**+ 次数不超上限 + 必须落到明确终态（不许停在 `pending`/`sending`）+ 台账不许是 `done` + 接收目录不许留下这个 transfer 的任何东西。`--fault=recv-readonly-lie`（只把"该落到哪个终态"换成 `done`）⇒ **恰好 1/22 报红**、退出码 1。<br>★ 两条**实测纠正**（都写进 harness 注释）：① 第一版把 `sending` 当终态 ⇒ 在第 4 次尝试的 33.2 s 处抓到 `{status:'sending',attempts:4}` 判红 —— **是判据写错，不是产品坏**（`sending` 是"正在投递"的中间态）；② 怀疑"崩溃后卡在 `sending` 就永远没人捞"→ 查码确认 `db/file_offline.rs:135-146` 的 `reset_sending_to_pending` 在 AppState 初始化时正是为这件事存在的 ⇒ **不是缺口，别重复登记**。 |
| 注入⑤顺手照出的**新缺口 A-10** | 这一格走的是「offer 期 `File::create` 就失败」那条分支（`file.rs:1604` → `transport.rs:3720`）：**只发 `FileReject{received:0}` + 记一条 error 日志，接收侧不写任何 DB 行、不发任何事件**。⇒ 发送方 43 s 后看到"失败"，**接收方完全不知道有人给自己发过文件，更不知道是自己目录写不进去**（磁盘满 / 接收目录设到只读盘是真实用户场景）。<br>所以本格的判据**只钉发送侧**，并在注释里写死"不许断言 B 侧记了 failed"（那行根本不存在，钉了就是空转）。补接收侧提示属于新增用户可见行为 ⇒ 登记 **A-10**，当前阶段不新增功能，**等拍板**。 |
| 注入⑥（改小轮）| `--fault=src-shrunk` = `npm run test:fault-injection:shrink`：先 `SIGSTOP` 冻住 B → **入队三行**（气泡 / 传输台账 / 队列，复刻 `send_file` 在点击那一刻写的产物）→ 把原件 `truncate` 到 4096 B → 解冻。**窗口是确定的，不是撞运气**：A 只在收到入站帧时才读盘（把 A-9 的实测结论反过来当机制用，**不等于认可那个缺口**，A-9 该修还是要修）。**22 断言 / 2 连绿**，实测：解冻 → 两侧 done 3.5 s、A 日志 `offer-sending ... size=4096`、落地 4096 B、sha 与截断后的源一致、队列行已关、接收目录只有终名那一份。`--fault=src-shrunk-lie`（只换期望摘要）⇒ **恰好 1/22 报红**、且报在那条摘要断言上、退出码 1。<br>★ 这一格是 §七「错误 size」的**真实用户形状**：协议层那种"线上谎报 size"早就被 length+hash 判死了（无需再造），真正会发生的是**磁盘上的原件在排队期间被改**（用户在离线队列等的同时编辑了同一个路径）。它同时照出 **A-11**（发送侧的 size 事后没人更正）⇒ 本轮只用打印钉住分歧，写成断言要等修；判据形状已写在 A-11 那一行。 |
| 注入⑧（改口轮 · 照出 A-12）| `--fault=recv-dir-rotted` = `npm run test:fault-injection:rename`：**先预置真前缀 `.part` 再把接收目录改成只读** ⇒ 往已存在的文件里写照样合法（写权限看 inode），唯一会 EACCES 的是收尾那次 `rename` ⇒ 这正是⑤那条注释点名"要另开一条"的 A-5 形状。**改口轮 22 断言**（默认轮 16 + 本轮 6）+ `-lie` 恰好 1/22 报红。★ 这一格两次改口：修之前"交叉自洽"只能打印（门禁没有"容忍已知红"这一档，park 一条红等于把门禁弄红；假 PASS 则是 §十禁令），修之后按 roadmap A-12 那格抄回来的原文加回断言，并补一条防止那条蕴含式在前件永不成立时永远为真。<br>`A 终态 {status:gone, aT:done} · B=failed · .part=1048576（整份）· final 不存在` ⇒ **发送侧宣布完成并删了队列行，接收侧只有改不了名的 `.part`**，`AlreadyHave` 按"字节数够了"直接短路，绕过了「rename 才算完成」。<br>⚠️ 边界：这一轮**是**接进了 `verify:e2e` 的 local 层的 —— 这不是妥协，是 `check-doc-numbers` 判据 D 的要求（每条正向轮次必须被门禁点名，否则"从门禁里删一条步骤"是静默的）。真正的边界在别处：① 判据**不预设"产品必须失败"**，只判"声称完成 ⇔ 磁盘有整份正确文件"这条自洽关系 + 本轮注入下可达的那个终态；② `-lie` 只翻一个期望值，所以红必须是**恰好 1 条**而不是全红；③ 解除只读之后 B 侧会不会自己把这一单补回来**只打印不设判据**（协议里没有"接收端自己重试收尾"这条码，写成断言就是替产品许愿）。 |
| 注入⑦（多文件轮）| `--fault=multi-file` = `npm run test:fault-injection:multi`：一次入队 **3 个 transfer**，其中**两单文件名完全相同、内容不同**（两份源放在不同目录里 —— offer 的 name 取自路径的 file_name，所以"同名不同内容"只能这样造）。钉的是 §七 两格的交叉：「连续多文件」+「磁盘已有同名文件」。**22 断言**，实测：入队 → 全部终态 **3.5 s**，落地 `photo-x.bin` + `photo-x (1).bin` + `note-x.bin` 三份；三单各自 done、队列清空、两侧台账各一行、每单记的字节数各自等于自己那张源、无 `.part` 残留。`--fault=multi-file-lie`（只把其中一份的期望摘要换掉）⇒ **恰好 1/22 报红**、报在「落地内容多重集合 == 源内容多重集合」那条上。<br>★ 判据为什么用**多重集合**而不是逐单比名字：落地名会被 `unique_path` 改名，逐单比名字就会退化成"对上了三份里的任意一份" —— 那正是串味能躲过去的缝隙。<br>★ **边界必须写清（这一格只测到一半）**：它证明的是「同一 peer 的 flush 是串行的 ⇒ 后一单 offer 时前一单已 rename，`unique_path` 看得见、会正确让位」。**没覆盖**"两个 offer 都在任一次 rename 之前到达"那个交错 —— 那种情形两单会拿到**同一个 final_path**、后一次 rename 直接覆盖前一次（`file.rs:1599-1602` 那段注释承认这个形状，但它只把 `.part` 改成按 transfer_id 命名，没解决 final 撞名）。⚠️ 这半句（"要造它需要**三个实例** ⇒ 记为边界"）在 2026-09-26 被 RCA 推翻，别照抄：**撞名这一格真正需要的不是第三个实例，而是"两份 `final_path` 在各自 rename 之前相同"这个形状**，而该形状可以在 L2 直接驱动生产收尾造出来 ⇒ 判据 `network/file.rs::two_same_named_transfers_do_not_overwrite_each_other`（先红：两行报同一个路径；修后绿，同模块 66 条 0 失败）。今天的口径：**串行那一半由 ⑦ 跨实例判着，撞名那一半由 L2 具名判据钉在生产收尾上**；L5 仍没有撞名这一格（同 peer 的 flush 串行 ⇒ 两个 offer 在两个实例之间根本叠不到一起，要线上重叠才需要第三个实例），这是**分层边界**，不是"未做"。 |
| 覆盖的**边界**（别把这句话读成"文件传输已覆盖"） | 只有「1 MB / 单文件 / 链路不断」+「前缀被污染」+「真前缀续传」+「100 MB 接收中杀进程」+「对端失联（冻结）后解冻自愈」这几格，外加注入④–⑧（目录写不进去 / 源文件被改小 / 连续多单其中两单同名 / 字节收完只塌在最后那次改名 —— 逐格口径见本节 A-2 行，别在这里重列）。<br>⚠️ **原来那句"全部未做"里有三处已经过期**，2026-09-26 按格拆开：这一族已**无“未做”格**（⚠️ 这里连着两次写“只剩 X”、两次都被推翻：第一次“只剩发送过程中杀进程 × 重启后继续”——注入③ 杀的是接收端，补成注入⑩ 后那句才重新成立；第二次“只剩三实例并发”在同一天收口，而**被证伪的是“需要第三个实例”这个前提本身**（见本节注入⑦ 行与 A-3 行）⇒ **“只剩 X”落笔前按格重数，且同一结论在纸上抄了几处就要改几处**）（上一轮这里写"只剩错 size"，本轮已补：chunk 级上限判据抽成纯函数 `chunk_exceeds_declared` 后拿到四条边界断言，理由与变异见本节 L-C 那格的 ⚠️）（**尺寸阶梯已按形状进本地层**：1 KB 单片 + 10 MB 多片带零头，各跑整趟默认轮 16 条断言，两档实测全绿）；**并发多文件**已由注入⑤覆盖（只覆盖"同 peer 串行 flush"这条时序，两个 offer 都在任一次 rename 之前到达那格要三实例）；**DB 锁竞争**已升 **L2**（两条动态判据 + 变异自证，见本节「§十二 DB 与锁」行）；**目标目录变化**按**形状**分两半各自已做 —— 目录仍在但不可写（`EACCES`）＝L5 注入⑧两个真实进程，整个目录被删走、接收句柄挂在被 unlink 的 inode 上、只剩最后一次 rename 能发现成品无处安放（`ENOENT`）＝L2 `network/file.rs::rotted_receive_directory_finishes_failed_never_done`（不是注入⑧的重复：同一个 rename 出口的另一半，且跑在**生产单聊收尾**上）。<br>⚠️ **「重复帧 / 乱序帧」这句要分两层说，别再笼统写"未测"**（两处都曾被我写反）：**L2（单元/源码守卫）已有具名证据** —— `file_relay.rs:337 duplicate_chunk_is_ignored_not_double_counted`、`network/file.rs chunk_seq_rule_only_rejects_real_gaps`（重复/迟到片=忽略，跳号才报错）、`group_receive_rejects_gap_and_duplicate_seq`、`transport/ble_framing.rs out_of_order_chunks_still_complete` + `duplicate_and_partial_do_not_complete`、`network/file.rs stale_attempt_frames_are_filtered_but_legacy_frames_never_are`、`favorites_tests.rs:1675 duplicate_transfer_id_offer_is_idempotent`。<br>**L5（两个真实进程之间、线上真的收到重复帧）没做**，而且判断是**不该做**：harness 要往对端塞一帧就得伪造 E2EE 封装与签名 ⇒ 造出来的东西生产码路径不会走 = 假绿（同"断链"那格的推理）。⇒ 这两格按 §十 记成 **L2 AUTOMATED / L5 不可自动**，不是记成"未测"。<br>⚠️ 唯一一条**今天真的没有行为测试**的形状：「第二次 FileDone 不许再改一次名 / 再写一条库 / 再发一次事件」。它今天的保证是**结构性的** —— `finish_receive` 用 `recv.remove()` 把接收器摘出来，第二次拿不到 ⇒ 进 `Ok(None)` 只补 Ack。`lib.rs` 那条 `duplicate_file_done_still_answers_with_an_ack` 是**源码守卫**（注释自述：没有能驱动 `handle_message` 并捕获出站帧的异步夹具），它管"两个出口都得回 Ack"，**不**管"不许二次收尾"。⇒ 记为边界，**不为它单独加 guard**（§十四：不要无意义增加 guard 数量；这条已由 `remove` 的形状 + `finish_receiver_into` 拿不到表 双重挡住）。<br>⚠️ **「断链」这一格为什么没做（不是忘了）**：单台机器无 root 造不出"只断一条链路"，而用中继去伪造会被 route 优先级绕过（LAN 在飞时根本走不到中继）⇒ 跑出来的绿是假的。<br>⚠️ 冻结轮顺带证出的 **A-9**：失联窗口内发送侧**一次都没尝试** ⇒ 现在这条"连接恢复"只被证明成"对端回来后碰巧有链路事件带动"，**不是**"到点自己重投"。<br>⚠️ 一条实测排除掉的注入方向：**改 A 侧 DB 里存的 sha256 不是一条可用的注入** —— 发送侧的摘要是在发送时从磁盘重算的（`sha256_file_hex`），DB 那个值上不了线。要制造"线上 hash 不符"只能用脏前缀这类**接收侧**注入（本轮就是这么做的）。<br>另：`--negative` 的反向自证**只钉得住文本那条腿**（2026-09-26 实测过机制，别再抄这句里的旧猜测）：链路照常建立，第 5 步投递断言报红，**实际超时是 60 s，不是这里曾写的 120 s**；红完就 fail-fast ⇒ **文件腿那两步根本不跑**，所以文件腿的"没送到"**结构上没法靠 `--negative` 演示**。⚠️ 但"演示不了"≠"没覆盖"，别把它写成欠账 —— 文件腿的"不许假成功"是由注入轮**正面**判着的（④ 失联期间两侧不许 done / ⑤ 写不进去必须落到明确终态 / ⑦ 目录只读 / ⑧ 只剩最后一次 rename 塌 / ⑨ 冻结期盘上一字不涨 / ⑩ 崩溃不许抹掉已入队），且每一轮的 `-lie` 都真的报红。★ 还有一条**退出码的账**：`--negative` 按设计是"红在屏幕上但 `exit=0`"（"如期报红"就是它自己的通过条件，实测 `NEG_EXIT=0`）⇒ 不要把它的 0 读成"正向绿"，也不要反过来把它"修"成非 0。|

七个 harness 自己的 bug／教训（值得记，因为它们现场长得像「产品坏了」，而且**其中四个是"假绿/假红"级别的**）：
1. 断言读错了文件 —— 查 A 侧链路却去读 B 的日志、还拿 B 的 id 当目标，于是永远查不到；
   表现是「链路超时 90s」，看起来完全像产品没连上。另一处是 `127.0.0.1:undefined` —— 种子拿的是身份对象，里面没有 `port`。
   ⇒ **教训：报红的文案必须说清「我读的是哪个文件、找的是哪个 id」**，否则下一轮只能靠猜。
2. **「二进制不得比源码新」守卫拦下过真问题，也自己造过一次假红。**
   第一次运行就拦下一个 9-20 的 release 产物（正是§十四要的形状：判据不靠人记得）；
   但后来它把 E2E 挡在门外 —— 护栏注入器写回时**只挪 mtime、内容不变**，于是"源码比二进制新"永远成立、直接 exit 2。
   ⇒ 改成**内容判据**：`git status --porcelain -- src-tauri/src` 干净 **且** 二进制晚于 HEAD ⇒ 放行并打印理由。
   **通用教训：拿 mtime 当证据的守卫，会被任何"写回但没改内容"的工具打穿。**
3. **假绿（最严重的一个）**：终局判定原先来自「有没有步骤抛异常」，
   而 `check(name, false, …)` 是不抛异常的 —— 所以一条报红的断言可以被写成 `PASS` 的 summary。
   ⇒ 改成**终局由台账推导**（`anyFail()`），并且这条修法当场被验证有效：
   我把一条**错的**契约（发送侧终态 `sent`）写进断言后，跑出来是 `✗ 1/16 条断言报红`，而不是假绿。
   另：反向模式加了「红必须落在建链之后」的门（`stepIdx > linkStepIdx()`），
   否则「B 根本没起来」这种基础设施噪声也算自证成功。
4. **存在性断言 = 半个断言**：J2 第一版写的是"A 侧存在一行 `file_transfers`"，
   而这一行是网络层自己建的，**传输根本没发生它也在** ⇒ 接近空转。
   已改成钉 `status` 与 `progress` 的具体值（见 §11.7.1 第 3 条）。
5. **故障判据第一次写成了「我以为失败长什么样」**：脏 `.part` 那四条断言我最初写的是
   「这种注入之后两侧都不许 `done`」⇒ 跑出来 `✗ 3/20`，看起来像"产品对坏内容宣布成功"。
   读 A 日志才知道真相：第 1 次 attempt `ok=false`、10 s 后 outbox 重试、前缀被丢弃、整份重收、
   `ok=true` ⇒ 停在 `done` 才是**正确**终态。红的是我的判据，不是产品。
   ⇒ 改写成「只允许两种结局、不许交叉」的互斥蕴含（补齐 ⇒ 双侧 done + outbox 清；没补齐 ⇒ 双侧非 done），
   并**为这四条补一个"把输入换成错的也必须红"的模式**（`--fault=poison-part-lie`）。
   **通用教训：故障注入的判据要写成"结局空间"，不能写成"我预期它失败"；
   而"输入换错必须报红"是判据非空转的最便宜证法 —— 不用改一行生产码、不用重编译。**
6. **注入的时机不在判据手里 ⇒ 连红两轮（这次是时序，不是内容）**：100 MB 那单我照前两格的惯性在**停机窗口**预置 outbox 行，于是传输早在「起 A/B 等链路」那一步就结束了，判据去盯 `.part` 时只剩终名文件；第二轮又发现 `waitFor` 是 500 ms 粒度、抓不住 0.78 s 的在飞窗口。⇒ 教训：**「先有状态、再断言状态」在时间维度上同样是空转**，判据必须自己制造那个状态、自己控制采样粒度。
7. **被信号杀死的子进程 `exitCode === null`**：拿 `exitCode !== null` 判「死透了没有」永远等不到 —— 得看 `signalCode`。`stopAll()` 里那套写法能过是因为它同时 `once("exit")`，单靠轮询就踩空。


⚠️ 必须如实说的边界：本轮只证明了两台同机实例在 **LAN 路径**上跑通，
不等于 routed/Tailscale、BLE、中继也被这条 harness 覆盖（§19 的网络分组仍欠）。

#### 11.7.1 接 J2 时先实测到的三条前提（不写下来，下一位一定会踩）

这三条都是**先读码再写断言**读出来的，不是从文档抄的；每条都直接决定断言会不会"假绿"或"假红"。

1. **发送侧的终态是 `done`，不是 `sent`**（这条我先写错过一次，被 harness 当场纠正）：
   生命周期是 `pending → active → sent → done`。
   `sent`（`network/file.rs:827`）只表示「我把字节全写进 socket 了」，
   只有 `finalize_send_accepted`（`network/file.rs:1199`，收到对端 `FileCompleteAck`
   或 Offer 阶段直接 `received = size`）才写 `send` / `done` / `progress 1.0`。
   两侧终态**同为 `done`**，含义不同：接收侧的 done 由 rename 触发，发送侧的 done 由对端确认触发。
   ⇒ J2 因此钉 `done`：这正是总指令「不要因 TCP write 成功就认为已送达」的机器形状 ——
   任何实现只要「自己写完就罢」，这条断言就红。
   ⚠️ 反面教训：我最初从一条**被分页截断的 grep**（只显示 15 个 `upsert_transfer` 里的 7 个）
   推出「发送侧终态是 sent、两侧不许统一」，并把它写进了本节与验收矩阵。**「不存在」不能来自带分页的检索**；
   改用 multiline 一次列全 7 处 `"send",…` 才看见 1215 行的 `done`。
2. **预置行必须命中真实的取队判据**，否则 J2 会"静默不跑还报绿"：
   `list_pending_file_outbox`（`src-tauri/src/db/file_offline.rs:24`）要求
   `status='pending' AND next_attempt_at <= now AND attempts < MAX_FILE_OUTBOX_RETRIES(=5)` 三个条件同时成立
   ⇒ 预置固定写 `status='pending', attempts=0, next_attempt_at=0`，并让 `local_path` 指向真实存在的文件
   （缺文件只会白烧 attempts 配额，第五次就永久失败，红得看不懂原因）。
3. **发送侧的 `file_transfers` 行不需要 harness 预置**：`network/file.rs` 自己有 15 处 `upsert_transfer`
   （pending → active → sent），补发路径也在其中。第一版断言写成"存在一行"其实接近空转，
   已改成钉 status 与 progress —— **判据要能自己发现"没发生"，而不是只发现"没记录"**。

---

## 12. 第五步终审：§19 八个分组的逐条三态表（2026-09-26）

**口径**：`✅通过` = 有机器判据且本轮或近期实跑绿，并写出**判据在哪个文件 / 哪条命令**；
`⚠️未执行` = 这一格今天没有判据，写清为什么、什么条件下才做得对；
`🔶需真机` = 结构性无法自动化，只能进 `docs/acceptance/stability-smoke-matrix.md`。
**本轮只做了"取证"这一件事，没有为了凑绿而新增判据** —— 一张填错的终审表比空表更危险。

### 12.1 代码 —— ✅ 整组通过

编译 / 类型 / lint / fmt / Rust / 前端 / 护栏 / 契约**不是逐条各测一次**，而是同一入口一次裁决：
`npm run verify:full`（步数与用时由 `check-doc-numbers.mjs` 现算，别照抄这里的任何数字），
CI 的 macOS / Windows / Android 三条腿跑的是**同一个 `scripts/verify.mjs`**（§15 要求，见 A-6 行的 job 记录）。
⚠️ 唯一的口子：这一组的"Contract 全过"只覆盖**已经建门的**契约（API 表、事件表、域依赖、
不变量绑定、改动预算、文档数字、测试清单）。**DB 表结构没有门** ⇒ 记在 12.8，不在这条里混过去。

### 12.2 数据 —— ✅ §19 那五个短语逐格具名，§9 点名的"部分失败"也已补上判据（#59 / #60 同日收口）

判据全在 `src-tauri/src/db/migration_tests.rs`（下面每条都是**具名用例**，不是"有测试"）。
条数现算：`grep -c "^#\[test\]" src-tauri/src/db/migration_tests.rs`。

| 格 | 态 | 判据（点名到用例） |
|---|---|---|
| 迁移正确 | ✅ | `migration_v1_to_v6_full_chain`、`migration_legacy_user_version_0_with_tables_runs_all_steps`（逐版本点名新增列，不是"没报错"）、`index_on_a_migration_added_column_must_not_live_in_schema`（老库必须先补列再建索引，顺序反了就打不开自己的库） |
| 新库正确 | ✅ | `migration_fresh_db_gets_latest_version`（点名 12 张必需表 + 版本落 `DB_VERSION`）、`fresh_db_has_every_hot_query_index`（新库首启即有热索引）、`migration_zero_version_with_zero_tables_is_fresh` |
| 旧库正确、**不破坏旧数据** | ✅ | `migration_preserves_identity_data`（device_id + 两把私钥逐条查回）、`migration_preserves_message_data`（消息**条数**与**内容**、outbox 保留、`seq` 回填 > 0）、`migration_success_preserves_data_and_adds_columns`、`superseded_indexes_are_dropped_and_uniquity_survives`（删冗余索引后内联 UNIQUE 必须还挡重复） |
| **不产生幽灵状态** | ✅ | ⚠️ 上一版我错挂到 `db/clocks.rs`（那两条判的是并发唯一性，不属这格）。真正证据是这四条：`downgrade_refusal_writes_nothing`（拒绝降级时**零张表**被建，本机 schema 一旦写进新库就回不去）、`migration_refuses_downgrade`（必须是带类型的 `InitError::Downgrade{current:99}`，不许按错误字符串猜）、`tables_are_counted_before_the_schema_is_applied`（数表必须早于建表，否则 `is_fresh` 恒假 ⇒ 每个新库都重放整条链）、`fresh_schema_alone_has_exactly_the_migrated_shape`（"只跑 SCHEMA"与"SCHEMA + 整条迁移链"的形状必须**逐项相等**；它红过一次，抓到的是 `index idx_messages_conv_seq` 这一项真差集）；幂等另有 `migration_double_start_does_not_reapply`（连起三次）与被删索引**不许被 SCHEMA 造回来**那半条 |
| ★ **迁移中途失败**（§9 数据生命周期点名的"部分失败"） | ✅ **已补（2026-09-26 #59）** | 判据 `migration_tests::a_failing_migration_step_rolls_back_completely_and_never_advances_the_version`。注入点选 **v2→v3 的 seq 回填**，理由就是上一格撞的坑：`BEFORE UPDATE` 是**行触发器**，只有"确有行要被改"才触发 ⇒ `run_migrations` 返回 Err 这件事**同时证明了注入点真的在写数据**，不必再加"没装 trigger 时确实删/改到 N 行"的前置断言。判据五面：① 失败必须上抛（报错里要能认出注入的原文，否则分不清是注入挡的还是产品自己别的问题）② `user_version` 必须还停在 2 —— 前进一步就等于让下次启动**跳过这一步**，留下"版本号说升过、列却缺"的半吊子库，那比崩掉更坏 ③ 同一步里的 `ALTER TABLE` 必须**一起回滚**（SQLite 的 DDL 是事务性的，所以这句是可验证的事实）④ 原有 3 行消息一条不丢 ⑤ **拆掉注入源重放必须成功**并补上 seq + 回填 > 0（真实场景里"注入"就是磁盘/锁/权限抖动 ⇒ 这就是"重启后仍然正确"那一腿）。⚠️ ③ 与 ④/⑤ 互为正反：⑤ 证明 `column_exists(seq)` 在这张连接上**能为真**，所以 ③ 的取反不是空转。⚠️ 不走 `init()`：它会先 `execute_batch(SCHEMA)` + `ensure_post_schema_shape`（要在 messages 上建 `(conv_id, seq)` 索引），没有 seq 的老库那一刻就先失败 ⇒ 判据会红在错误的原因上。同时改掉了 `migration_success_preserves_data_and_adds_columns` 里"插入一条数据验证 rollback 后仍存在"那句**假注释**（它通篇没验回滚），并把真正的回滚判据指到新用例。 |
| ★★★ **#60 已落锤并已修（2026-09-26）**：v6→v7 的孤儿谓词锚错了列 | ✅ **RED→GREEN 都跑过，判据具名** | 先红后修，逐条**实测**（把 v7 那 6 条 DELETE 原样在一份同形状的临时库上跑，每条打印行数）：**2 条删掉在册群的数据 / 2 条报错被吞、从上线起一次没生效过 / 2 条本来就是对的**。`orphan_group` 谓词写的是 `id NOT IN (SELECT id FROM groups)`，但 `group_outbox.id` 与 `file_outbox.id` 是 `INTEGER PRIMARY KEY AUTOINCREMENT`（**行号**）而 `groups.id` 是 TEXT 群 id ⇒ 两个域永不相交，条件退化成"只要这行不是零行号就删"；`group_reads` / `pending_group_reads` **连 `id` 列都没有** ⇒ 稳定报 `no such column: id`，而 v7 是软失败风格（`if let Err(e)` 只 `eprintln`）⇒ 没人看得见。实测数字：`group_outbox 2→0`（在册那行 `m-live` 一起没了）、`file_outbox 3→1`（两条群行全没，只剩 `group_id IS NULL` 的 1:1 那行）、`group_reads 2→2` 且 `pending_group_reads 2→2`（**孤儿一行没清**）、`messages 3→2`、`group_recalled_messages 2→1`（后两条走 `orphan_conv`，锚的是 `conv_id`，**本来就是对的**）。<br>⚠️ 注释自称"保守口径"是**反的**：它比要求的宽到能吞掉全部在册数据。<br>**修法只有一处**：`orphan_group` 改锚 `group_id`（四张表都有这一列），行号/文本 id 之别就此消失；谓词形状、软失败风格、事务边界、`DB_VERSION` 一字未动（§十八：不借修 bug 改迁移框架）。<br>**判据**：`migration_tests::v7_orphan_cleanup_keeps_live_group_rows_and_drops_true_orphans` —— 种一个在册群（`groups` 有行 + 会话在）+ 一张真孤儿，版本退回 6 走真实 6→DB_VERSION 路径，**11 条断言**：在册四张表的行都必须活、孤儿行都必须死、外加 1:1 待发行一个字节不许动。**两面都钉住才不空转**：只断"活下来"会被"整表不删"混过去，而那两张没有 `id` 列的表恰好只有**孤儿侧**断言能证明语句真跑通了（修之前它就是静默失败）。<br>🔶 **一条如实留下的账**：已经跨过 v7 的库**不会重放**（`run_migrations` 只跑 `from >= current`），所以它们盘上仍留着"已删掉群"的待发已读回执 —— 而 `db/read_receipts.rs::list_pending_group_reads` 只按 `peer_id` 查、不看群还在不在 ⇒ 这是一只**活的幽灵**（对端每次上线都会被重新捞出来投递），不是死数据。要不要补一条 v9→v10 把这笔存量清掉 = **待拍板 #61**；本轮**不补**（§十八：清存量不是修这个 P0 的必要条件，而"它到底会不会被用户看见"我只有读码证据、没有实测证据）。 |
| 补这格的**正确做法**（含本轮踩到的两个坑，其中第二条**已被实测推翻并改正**） | — | ① 注入点必须自带"真的发生了写入"的证据。两条路：要么像上一格那样前置断言"没装触发器时这条语句确实删/改到 N 行"，**要么直接选行触发器** —— `BEFORE UPDATE` / `BEFORE DELETE` 只在"确有行被动"时才触发，所以"报错了"本身就是"写入了"的证据（本轮走的这条，省掉一半脚手架）。② ❌ 我上一版写"别指望 `run_migrations` 返回 Err —— v7/v8 是软失败风格" —— **半句是错的**：`db.rs:130` 是 `(step.run)(conn)?`，循环**照上抛**；被吞的只是 v7/v8/v9 **闭包内部**那几处 `if let Err(e)` 的逐语句容错。所以"这一步会不会被吞"要分成两层问：**语句级**（会被吞，#60 就是被它藏了 7 个版本）和**步骤级**（一定上抛，`a_failing_migration_step_...` 已钉）。③ 判"迁移中途失败"不许走 `init()`：它会先 `execute_batch(SCHEMA)` + `ensure_post_schema_shape`，没有 `seq` 的老库在那一步就先塌 ⇒ 红在错误的原因上；要照生产那一刻的状态自己摆表。④ 收尾顺序照旧：`cargo fmt --all` → 新用例过 `check-test-manifest`（先 `--update` 再 `--sync-baselines`）→ `npm run verify:full`。 |
| 边界（两条，其中一条是**上一版写错的**） | — | ① 我上一版说"全部跑在内存库"——**不对**：迁移一族走 `temp_db_path()`，是**磁盘上的真实临时文件**（连 wal/shm 一起清），只有形状恒等那条与 `db/clocks.rs` 那两条用内存库 ⇒ "迁移不坏盘上数据"这一层比我记的强。② 仍然成立的那条边界：**用户机器上真实老版本 `.db` 文件**没有被任何判据碰过 ⇒ 🔶 进 Smoke（矩阵"数据生命周期"组） |

### 12.3 网络 —— 只有两格真的是绿的

| 格 | 态 | 依据 |
|---|---|---|
| LAN | ✅ | 双实例 harness 真跑（§11.7），默认轮 + 8 条注入轮全在同一台机器两个进程之间 |
| 掉线恢复 | ✅ | 注入轮"对端失联后解冻自愈"（正向绿 + `-lie` 反向红），`npm run verify:e2e` 本地层 |
| 多链路（选路正确） | ✅ | 前端 `src/utils/channelState.test.ts` 钉住同一份选路口径；Rust 侧具名用例见测试清单 |
| Routed / Tailscale | ⚠️ **判据很厚（13 条具名），缺的同样是"跨实例只走这一条路真跑一趟"** | ❌ **我上一版这格写错了**（写成"⚠️未执行 / 无判据"）—— 同一个"凭记忆下笔"的毛病第三次抓到（前两格是 relay 与 gossip）。按门禁自己那份 Rust 清单现算，这一族有 **13 条**：`grep -icE "routed\|tailscale\|pick_link" src-tauri/test-baseline.macos.txt` ⇒ **13**。里面正是选路层的口径本体：`mesh::selection::tests::path_priority_lan_beats_routed_and_bluetooth`、`path_priority_routed_beats_bluetooth`、`path_priority_relay_beats_bluetooth_but_loses_to_routed`（三条优先级把 §三"链路优先级"钉住）、`unhealthy_lan_does_not_block_healthy_routed` + `network::transport::tests::route_order_skips_unhealthy_lan_when_routed_is_healthy` + `route_order_prefers_lan_over_routed_regardless_of_insertion`（健康度与插入序都不许翻转优先级）、`only_routed_connection_still_dials_lan_path`（**只有 routed 时仍会去拨 LAN** —— 这就是"只走 routed"那个场景在单元层的形状）、配置解析四族 `discovery::routed::tests::*`（端点 JSON 往返 / 畸形输入退空 / device_id 可选兼容 / 裸 IP 与显式端口）、`commands::tests::routed_endpoint_address_is_normalized_to_ipv4_socket`、`tests::relay_circuit_is_tagged_relay_not_routed`（中继与 VPN 不许混成一类）。<br>⇒ 真实缺口收窄成一句：**没有一次跨实例、且 LAN 不可用的真跑**（§五 的 `LAN + Tailscale` 组合那一格）。这属环境不属代码 ⇒ 🔶 平台 Smoke，不写成 PASS。⚠️ **与 §12.4 gossip、§七 最后一格是同一块欠账**：都要"第三个真实对端 / 非 LAN 出口"，一起补，别拆成三块账。<br>**2026-09-26 #64 把这一格往前挪了一小格，但没挪到位，别读成"已绿"**：默认轮的"起 A/B"步骤新增一条判据 —— 这一对实例之间**至少一侧**打出过 `建链 … path=routed`（两侧合计 ≥1；实测 11/11 轮里恰好一侧拨出、另一侧只看到入站 ⇒ 按侧断言会漂）。反证是把两端预置的 `routed_endpoints` 清空（`E2E_NO_ROUTED=1`，改判据读的那个输入而不是改判据）：新那条超时红，而**旧的那圈 `peer=<id>` 判据照常过** ⇒ 这就是"以前这条拨号坏了 12 轮也全绿"的实测证据。<br>⚠️ 它**仍然不是**"只走 Routed 那一趟"：出口还是回环（LAN 广播同时在跑，选路走的极可能是 LAN），所以本格态保持 ⚠️，欠的还是同一句话。 |
| BLE | 🔶 | 常量层有门（`check-ble-constants.mjs`），**通信本体只能真机**；已在 Smoke 矩阵标 MANUAL-HARDWARE |
| Relay | ⚠️ **判据很厚，缺的是"跨实例真跑一趟"** | 我上一版凭记忆写成"判据存在与否未复核"——**已按门禁自己那份清单复算并推翻**：`grep -ic relay src-tauri/test-baseline.macos.txt` 现算（口径=测试路径含 relay 的条数，含 `file_relay::` 接收侧、`mesh::relay_policy::` 转发真值表、`mesh::router::`、`commands::relay_config_tests`）。里面有**安全边界级**的几条：token 不进探针报告、配置 fail-closed、`offer_without_chunk_size_is_refused_instead_of_buffering`（P4 那刀的判据）、`unsafe_transfer_id_cannot_create_a_file_outside_the_dir`（路径逃逸）、重复分片不双计、零字节能收尾。**没有的那一件**：两个实例经中继真通一次。⚠️ 2026-09-26 改判：缺的**不是**"一台活着的中继服务器"
（它就在本机 `~/Documents/code/gosslan-relay-server`），而是"一条被封的直连"——
`relay.rs:104` 会把**已有任何链路**（不分通道）的对端整个跳过，同机两实例靠 LAN 一定连得上
⇒ 对端进不了中继候选，且仓库里没有只关 LAN announce 的开关。真跑需要 root 防火墙或第二台真机
⇒ 按§十记 **Smoke-9**（见下表与 §12.7 的因果链），**不升 AUTOMATED** |

### 12.4 消息 —— 混合，缺口是同一条

发送 / 送达 / 重复 / 离线补发 ✅（harness 默认轮断言两侧同一条 `msg_id` 只出现一次 + outbox 清空；
Rust 侧 `enqueue before deliver`、幂等 Ack、解密失败留空 `msg_id` 均具名）。
**已读** ✅（判据唯一性有门；跨实例点亮链路上一轮刚修过竞态）。
**gossip** ⚠️：判据比上一版记的多得多（`grep -ic gossip src-tauri/test-baseline.macos.txt` 现算，
含信封签名/篡改检测、TTL 保留、fanout 排除发送者、群信封重签覆盖创建者与成员、
`direct_and_gossip_same_msg_id_persist_single_row`（直连与 gossip 同 id 只落一行）、
非成员不提前 bail、解密前去重）——⚠️ 这句"全部在同进程内"**在 2026-09-26 之后只对一半**：
`--round=group` 已经让**一条真实 Gossip 信封跨实例投达并被解密落库**（含撤回 G-Set 物化），
仍然缺的是**多跳收敛**那一半 —— 三实例、其中一个成员离线再上线的那一趟真跑，而它恰好
**曾和 §七 最后一格共用同一套三实例基础设施** —— 这句话今天塌了一半：§七 那一格收口时**根本没用第三个实例**（RCA 见 §12.9 / A-3 行）⇒ 三实例地基现在**只剩这一处欠账要用**，“两处共用一块”不再是省一次投入的理由。
**⚠️ 上面那句"仍缺多跳收敛"在 2026-09-26 同日 #75 之后过期**：那一半已经由 `--round=gossip3` 补上（发版前层 `npm run verify:release`，判据 = "A 的逐成员直发队列里从来没有面向 C 的那一行" + C 侧四条落库判据，正向 25 断言全绿 / `-lie` 恰好 4 红，详见 §12.7 末）。同一轮实测出的**新产品缺口不是这一格的欠账**：中间人只在收到的那一瞬间扇给**当时可达**的邻居 ⇒ **晚到的群成员拿不到补推**（已另立待拍板，别把它读成"§12.4 gossip 已全绿"）。
**异常（解密失败不 Ack）**：⚠️ 见 B-1a-P03。**2026-09-26 按现物重读，这一格的缺口比这里原先写的窄，也比我原先说的更难**：
- **落库那一半早就有具名判据**（不是我原先说的"整格没判据"）：`db::tests::failed_decrypt_leaves_msg_id_free_for_the_later_good_copy`
  + `ack_from_non_recipient_is_rejected` + `failure_ack_marks_recipient_failed`（`grep -nE "failed_decrypt|ack_from_non_recipient|failure_ack_marks" src-tauri/test-baseline.macos.txt` 现算）。
- 真正没有的只有**"不许发出 Ack"这半边**，而且它**不是"补个夹具"就能到手的**：
  `state.rs:873-874` 里 `AppState { pub app: AppHandle, … }` 是**具体 Wry 运行时**：结构体自身确实是泛型
  `AppHandle<R: Runtime>`（`tauri-2.11.5/src/app.rs:386`），但**裸名**由同文件 `:384` 的
  `#[default_runtime(crate::Wry, wry)]` 展开成 `AppHandle<Wry>` ⇒ 单开 `tauri::test` / 换 `MockRuntime` **也不通用**（两个不同类型）。
  所以任何走 `handle_message` 的行为测试要么把 `AppState` 泛型化（**动生产类型，全仓最热的一条路径**），
  要么在测试里起真的 Wry 应用（CI 里起不来）。
- ⇒ 处置：与 §七 第 7 步"把 `handle_message` 分册 / 拆 store"同批**暂缓**（当时的判词是"纯搬家、短期只降稳定"，
  这条只是它的一个下游）。**别把这条读成"再写一个测试就行"**：先要一个"愿意为了可测性动这条路径"的点头。

### 12.5 文件 —— ✅ 是本表最完整的一组

尺寸阶梯 / 断线 / 续传 / 重启 / hash / 重复帧 / 乱序 / 旧 attempt / 错 size / 错 hash / 磁盘同名 /
脏 `.part` / 目标目录变化（拆两半）**逐格有具名判据**，明细就在 §7 与验收矩阵第 12 行，不在这里重抄。
剩余：**无（三实例并发那格已于 2026-09-26 收口，见 12.9 / A-3 行）** ⚠️ 这一句历史上被"还剩几格"的总数漂移坑过两次，落笔前按格重数。
⚠️ 这句曾写漏过一格：「发送过程中杀进程 × 重启后继续」当时也没有判据 —— 注入③ 杀的是接收端，
没有任何一轮从"发送端自己崩了"那一侧看过，2026-09-26 由注入⑩ 补上）。

### 12.6 UI —— 静态契约层比预期强，运行时层是空白

首帧 / 骨架 / 主题 / 每窗口只带自己的骨架 / 骨架撤除按类而非按清单 ✅
（`src/utils/windowEntries.test.ts` 一组，含"改回逐个 id 清单就 FAIL"的自证；
`appearance.test.ts` 交叉核对首屏脚本与 `utils/appearance` 逐例一致）。
移动布局 ✅（`storeContract.test.ts` 钉 isMobile 不等 IPC 就红，#27 那次真缺陷的形状）。
冷启动"点到了"可见 ✅（`windowLaunch.test.ts`）。
**点击响应 / 焦点 / 常驻 / 通知 / 预览的实际观感** 🔶：
这些判据全是**源码/结构级**——闪白、字体重排、通知不弹、窗口层级错，静态扫不出来。
本轮**没有**跑任何浏览器驱动的 UI 断言 ⇒ 运行时那一层只能真机验，别把"结构级绿"当成"看起来对"。

★ **这一格为什么空着：本轮把"能不能自动"实测了一遍，答案是"今天不能，缺的是地基不是断言"（登记为待拍板 #78）**

三条事实都是这一轮现量的（复跑命令挂在每条后面）：

1. 前端 60 份 `.test.ts` 全部由 **`node --test --experimental-strip-types`** 直接跑 —— 没有 DOM、没有组件挂载，
   而且**仓里根本没装任何测试框架**（`node -e` 读 `package.json` 的 deps∪devDeps，搜
   `vitest|test-utils|jsdom|happy-dom|jest|playwright|testing-library` ⇒ **零命中**）。
   同一条脚本顺手对账了名单 ↔ 实存文件：**60 / 60 两边相等，没有"写了但永远不会跑"的测试**
   （我原本怀疑这里漏了，量下来是我猜错）。
2. `src/api/index.ts` 第 3 行就 `import { invoke } from "@tauri-apps/api/core"` ⇒ **在普通浏览器里打开
   `npm run dev` 的页面会因为缺 `__TAURI_INTERNALS__` 直接失败** ⇒ "用 Playwright 驱动真界面"这条路
   不是配置能打通的，**必须先有一层 IPC 替身**。
3. 全仓 `mount(` 只出现在 **1 个**文件（`src/utils/designGuards.test.ts:1065`），而那一条命中其实是
   `src.indexOf("onBeforeUnmount(() => {")` —— **在源码文本里找生命周期钩子，不是渲染组件**
   （`createApp` / `render(` 在测试里零出现）⇒ 现在这一层**一条真渲染出来的 DOM 断言都没有**。

三条路各自的代价（都动 dev 依赖或系统授权 ⇒ 按§十八属"要用户点头"，我不自己选）：

- **A 装 DOM 环境 + 组件测试工具**（jsdom/happy-dom + `@vue/test-utils`）：最便宜、能进 `npm test` 那一层、
  CI 照跑；判得住"点一下 → 界面状态变了 → 只调了该调的那条 IPC"（覆盖 §19 UI 的点击响应 / 弹层互斥 / 移动布局 /
  任务与预览换内容）。**判不住真实 WebView 的观感**（闪白、字体重排、窗口层级）⇒ 别把它写成"UI 运行时已覆盖"。
- **B 用 macOS 无障碍接口读真窗口的控件树**：零新 npm 依赖、判的是真进程真界面，能补 A 够不到的那几格；
  代价是**要给跑测试的进程开"辅助功能"权限**（动用户系统的授权，不是我该替他点的开关），且只有 macOS 腿。
- **C 维持现状**：按 §十 明确挂在 MANUAL-HARDWARE，不假装绿 —— 代价是 §19「UI」这一组会一直留一格不成立，
  也就是 §12.9 那句"不能宣布最终稳定版达成"的三条之一。

我的建议：**A 先做**（不碰系统、能进门禁、覆盖 §19 UI 那 9 项里的大多数），B 只在 A 覆盖不到的"真实观感"上补，
且必须用户自己点头开权限；选 A 之外什么都不动 ⇒ 在此之前 §12.6 那行 🔶 **一字不改地留着**，不许因为"打算做"就提前改绿。

### 12.7 自动化 —— ✅，但要写清"自动化"的边界

双实例 E2E、故障注入、边界测试、回归测试四类**都在同一入口里**（`verify:e2e` 本地层）。
高风险交叉场景 ⚠️：矩阵里点名的组合（聊天+大文件、群+文件、任务+窗口复用、通知+冷启动…）
**现在真跑过两族**：文件那一族（**逐格清单见本节下方与验收矩阵「轮次账」，这里不写"几格"**——
总数型声明没有守卫，本节 A-2 行已经为这条立过规矩，这句当时漏改）+ **群聊那一族**（2026-09-26 补 `--round=group`，
一次覆盖两格组合 —— 「群聊 + 离线成员重新上线」（入队时对端进程还没起，只能靠建链后 flush 补发）
与「聊天 + 群聊 + 文件 共用同一对实例且不串味」）。其余仍是单点判据；
**三实例那一类**现在只剩**群 gossip 多跳收敛**一格 —— 曾与它并列的“两个 offer 抢同一次 rename”已于 2026-09-26 收口，且收口方式正是**不需要第三个实例**（见 §12.9 / A-3 行）⇒ **已拍板（2026-09-26）＝建这一轮，但挂在"发版前"那一层，不进日常本地门禁**：日常本地层实测 14 步 / 452.7 s，再挂一个常驻第三实例会拖慢每一次回归，而这一格也确实该被机器反复证明 —— 两边都要，就把它放到发版前。落点＝新 `group:"release"` + `npm run verify:release`，并把这条命令**写进 §二十 的发版检查单**（`docs/acceptance/1.0-release.md`；`semver.mjs release` 本身不跑任何门禁，所以落点是检查单而不是脚本），`verify.yml` **不认领**（release 产物 + 桌面 GUI，CI 跑不动，理由同 §12.7 那条 LOCAL_ONLY_WHY ⇒ 宁可不跑也不许把没跑写成绿）。

**已落地（同日 #75，`--round=gossip3`）。判的东西比原计划窄，两条实测边界必须写清：**

- ✅ **正身判据不是"拓扑"，而是"A 的逐成员直发队列里从来没有面向 C 的那一行"** —— 这一条在 outbox 的任何生命周期阶段都成立（发完删行也不会翻转），所以它是"C 收到的不是直发"的可复跑证身。C 侧再钉四条：落库恰好一行、正文是解密后的明文、发送者仍是 A（经手不改归属）、`conv_id`/`seq` 与信封一致；外加"B 自己仍只有一行"（递给下游不让自己重复落库）。
- ❌ **原计划那句"★ 非空转的前置 = C 的日志里没有与 A 建链的任何一行"经实测不成立**，两次都红：`run-2026-09-26T09-20-42-139Z`、`run-2026-09-26T09-24-08-894Z` 里 C 库 `lan_enabled='false'`、好友只有 B、端点只有 B，A 的日志仍出现 `diag/announce_verified: from=<C 的 id>` ⇒ **关掉局域网发现既没停止广播、也没停止接收侧的 announce 验证**，同机三实例必然互相建链。拿它当前提＝这一轮常红。**产品侧那条另立待拍板**（不在测试任务里顺手改产品码，§十八）。
- ❌ **"让 C 在 A 停机之后才第一次上线"这个绕法也不行**：两轮实测 C 侧 90 s 内 **0 行** ⇒ 中间人只在"收到的那一瞬间"把群 gossip 扇给**当时可达**的邻居，**晚到的群成员拿不到补推**。所以这一格判**多跳收敛**、不判"晚到补拉"——后者是一个**新的产品缺口**（记进 §五矩阵与待拍板），在这一轮里写成绿就是替产品许愿。⇒ 三端**同场**起。
- **两面实测（同一份判据）**：正向 `test-results/run-2026-09-26T09-34-04-595Z` ⇒ 链式轮全绿（条数只写在验收矩阵顶部「轮次账」，本步自己 7 条、其余是它复用的基础旅程）；反向 `--round=gossip3-lie`（只把判据要读的那个 `msg_id` 换成必定不存在的值）⇒ `test-results/run-2026-09-26T09-34-17-793Z` **恰好 4 条报红**，而"A 无面向 C 的行"与"B 仍单行"两格照常绿 ⇒ 红只来自读 C 库那四条，非空转成立。
- **三实例的地基顺带补齐**：备份/还原、`after-*.db`、失败时的日志关联一律从"点名 A/B"改成按**本轮可能被启动过的实例清单**走 ⇒ 用户本机原有的 `gosslan-3.db` 现在每轮都会被备份并在结尾还原（跑完打印「已还原用户原有实例库 3 个」；这一格在补之前是真危险：第三实例的库从来不在保护范围内）。
- **顺手抓到一条 §十六 真 bug**：`check()` 的 `expect`/`actual` 为 `undefined` 时会被 `JSON.stringify` **整个键丢掉** ⇒ 报告里"预期/实际"两栏凭空消失（上面那个反向 run 里三条正是如此）。已改成显式渲染 `<无此行/undefined>`，正反两面各复跑一次确认报告不再有空栏。
- **这一层的实测成本**：`npm run verify:release` = **1 步 / 12.7 s**（`test-results/verify-release-1.log`，含三次进程启停）。
  ⇒ 分层的理由**不是**"它慢"（它比本地层任何一步都快），而是**用户 2026-09-26 明确拍板"只挂在发版前"**，
  以及"多开一个真进程 + 多一轮重启"这类基础设施一旦进了日常层就会变成常驻负担。别把这句读成"以后可以顺手并进 local"。
- 生产侧扇出候选取自 `reachable_neighbors` 而不是 `peers` 知识集，这正是审计 P0#7 的失效形状，已由 `gossip_fanout_targets_reachable_links` 钉住源码那一半，本格钉的是"真跑一趟"那一半。

★ **这一轮顺手照出两条产品事实，都不在测试任务里改码（§十八），登记为待拍板：**

- **#76 关掉局域网发现之后，LAN 那条门其实没关。** 现象（实测两次，都是同一形状）：把某个实例的"局域网发现"开关关掉、好友与端点都只留一个，它**照样**在同一台机器上被别人学到、被别人拨进来，两侧日志都记成 LAN 链路。⇒ 用户界面里那个开关的**实际语义**比它名字窄（只关"主动发现"，不关"广播 + 接收侧验证"）。要不要把它改成"关掉就真的不广播也不接"= 待拍板；**注意这条如果被当成产品语义写进文档会误导用户**（"我关了局域网，为什么同网段的人还能看见我"）。
- **#77 群消息只在这一刻扇给"当时连得上"的人，晚到的成员拿不到补推。** 现象：同一条群消息，中间人收到并转给了当时在线的成员；另一个成员**之后**才上线 ⇒ 90 s 内它的库里 0 行，而它已经是群成员、也有群密钥。1:1 那一侧有"重连后 flush 补发"（`--round=group` 已钉），群 gossip 这一侧**没有对应的补推路径**。⇒ 这决定了"群里的一条消息是否最终一致"的真实边界；改不改、怎么改（缓存 TTL / 新链路建立时重扇 / 让下游主动补拉）= 待拍板。

**§十六 产物层自己的账（#65，2026-09-26）**：报告目录以前缺的就是 `screenshots/` 那一格 ——
其余产物（summary.json/html、两端 app.log、DB 快照、recv/）早就每轮在生成。
现在每轮两张全屏 PNG（链路建立后 / 两端重启后），判据只钉"落盘 + **PNG 结构成立**（签名 + IHDR 宽高 > 0）"，
**"界面长什么样对不对"仍归 §12.6 的结构级与 MANUAL**：这一格里没有被机器验证的东西就是没有被验证的东西。
判据自身配三格自证（假 PNG 判假 / 缺文件判假 / 真截图判真）+ 独立入口 `test:e2e:shot-selfproof`；
反证是 `E2E_NO_CAPTURE=1`（关掉采集器）⇒ 默认轮恰好 1/18 红、同步骤其余判据照常绿。
★ 顺手记一条自纠：第一版在"采集器被关掉"时仍打印"能判真"，而那一格当时根本没跑 ⇒ 文案拆开重做。
⚠️ **"结构成立"这个口径是被推翻一次才改来的**（本段此前一直写的是第一版那句"≥200 KiB"，已按现状改口）：
按体积定阈值时，一次近乎空白的桌面只截出 106,973 B ⇒ **pre-flight 把整轮 E2E 拦在起跑之前**，红的是判据不是产品。
⇒ 通用一条：**判据的输入要选环境不变量（结构），别选随屏幕内容摆动的量（体积）**。
⚠️ Windows 接上之后这一格会**明着红**（采集器只有 macOS 实现），不是静默跳过。
★ 顺带量到一条**新的运维事实**（#65）：加了截图之后每次本地层会多写一批位图，而**这套产物当时没有任何保留策略**（本段末「处置已拍板＝选 C」那句已把它补上）。
⚠️ **但 #65 当时把归因写错了，本次（2026-09-26，#69 收尾时）实测推翻**：
`test-results/` 现在 **8.9 GB / 216 个 run 目录**（#65 记的是 2.6 GB / 113 个），按类别拆开是
**收发用大文件副本 4.2 GB + 4.5 GB ≈ 98%**、两端 DB 快照 108 MB、今天 156 份的截图 114 MB。
⇒ 胖子不是截图（一两个百分点），是每轮留下的那两份临时造的 100 MB 文件；
"截图把量级放大一个数量级"这句不成立，"一次本地层多写约 44 MB"也不成立（大头一直来自文件轮）。
处置已拍板（2026-09-26）＝**选 C，同日已实现**：绿轮之后删掉那两份可再生的大文件副本，其余证据（HTML/JSON/日志/DB/截图）全留；
红轮一字不动地全留（删了就等于毁取证现场）。A/B 不采纳。
**实测两面（同一份判据）**：正向轮 **18 条全绿** ⇒ 打印「已删 src + recv，释放 2.0 MB」，产物目录只剩 **4.1 MB**
（两端日志 / DB 快照 / 截图 / summary 全在）；`--fault=recv-dir-rotted-lie` 那轮**恰好 1/24 报红** ⇒
`src/` 与 `recv/` **两份都还在**（8.1 MB）⇒ 这条清理读的是真判定，不是无条件删。
判据形状照 §十四 定的那条走：纯函数 + 四格自证（绿⇒删两样 / 红⇒不删 / 反向模式⇒不删 / 报告自己不合格⇒不删），
自证任一格不成立就**一律不删并把这轮判红** ⇒ 坏的方向只会是"多留"，不会是"删掉取证现场"。
★ 同日 #73 补掉这条边界：删完之后把 `{policy, freed_bytes, deleted}` **回写进刚落盘的 `summary.json`**，
并**用同一份 §十六 契约再判一次**（回写要是把契约字段弄丢就当场判红）⇒ 现在机器读得回"这轮删没删、省了多少"。
实测两面：绿轮 `retention = {policy:'C', freed_bytes:2097152, deleted:['src','recv']}` · `verdict=PASS` · `exit 0`；
红轮 `freed_bytes:0 · deleted:[]` · `verdict=FAIL` · `exit 1`，且 `src/` 与 `recv/` **两份都还在**。
★ 本地层在 `5936c7e` 上整层重跑过一次（#71/#73 落地后的第一次全量）：✅ --group local 那一组通过（14 步，共 495.8s），层内 ❌ 标记 0 处；⚠️ 我那条 `LOCAL_EXIT=${PIPESTATUS[0]}` 在管道后取到空（同一条命令里 tail 先吃掉了它）⇒ **退出码这一格今天靠的是"那一组通过"这行 + 后台任务自己报的 exit 0，不是我 echo 出来的数** ⇒ 保留策略（只有判绿才删）没有把任何一轮判成假绿。

**§十六 报告契约从"我核对过一次"变成"每轮自己核对"（#69，2026-09-26）**：
上一轮我手工核对产物字段时，先误报"缺日志关联"（那格其实只在有失败步骤时才挂，且 `--negative`
那轮 0 命中行本身就是结论），核对过程中真查出来的却是**另一格**：
`summary.html` 的步骤徽章是一个三元式，`verdict` 缺失（fail-fast 跳过的步骤）时落到 else ⇒ **渲染成 ✅**。
也就是说那份报告把"根本没跑过的步骤"显示成通过 —— 正是 §十 明令禁止的"把没有测试伪装成 PASS"，
只不过这次被伪装的是报告自己。实测锚点：`run-2026-09-26T06-53-47-012Z/summary.json` 里第 5、6 步
只有 `{name}` 一个键。
修法两格，都在 harness 里、**产品码一行没动**：
① 落盘的每条步骤强制有终态（没跑到 = `NOT-RUN`），徽章多一档 `⛔ 未跑`，页脚写明"未跑不许算进通过格"；
② 新增对**产物**的契约判据（不是对源码字面量的存在性检查）：`verdict` ∈ PASS/FAIL、`trace` 带
`msg_id`+`transfer_id` 两个键、`duration_s` 是数、每条步骤有终态、每条断言带 步骤/预期/实际/PASS-FAIL、
**报了 FAIL 且有失败步骤就必须至少一步带"日志关联"**。每轮 `writeReport` 之后把**刚写的那份**读回来判，
不合格就在清理之后**盖过上面的退出码**改判 1（包括反向模式那条"按设计退 0"）。
判据自身配 11 格自证（字段齐全判真 + 逐格缺字段判假 + 徽章三格；**#79 之后是 14 格**，见本节下方那一格），独立入口
`node scripts/e2e-multi-instance.mjs --report-contract-selfcheck`；
另一入口 `--report-contract=<run 目录>` 拿同一份判据读任意一份已落盘报告（不需要 release 产物）。
★ 反向自证走的是"只换判据的输入"：把生成器里 `trace` 那一行注释掉 ⇒ 同一条 `--negative` 轮
断言层照旧打印"反向自证通过"，而**报告契约**判红、退出码从实测 0 变成实测 **1**。
恢复那行后同一轮实测：契约 ✅、退出码 **0**、`summary.html` 出现 3 处"未跑"字样。
⚠️ 旧产物不追溯判红（历史 60+ 目录是旧代码写的，`duration_s` 那类字段本来就没有），
判据只在**每轮生成时**生效 —— 它防的是"以后少一格"，不是"以前少过一格"。

★★ 顺着这条契约判据又抓到**更隐蔽的一格（#70，同日）**：报告"失败时沿链追踪"用的日志过滤键是**手写四个变量名**
（`msgId/xferId/xferId2/xferId3`），而 6 个注入轮各自另造 id（`xferId4…8`、群聊那三条）⇒
**注入轮那一单在日志里根本进不了报告**，而 `trace.transfer_id` 还指着同一轮里 J2 那一单 —— 追踪会指到**错的那一单**。
实测锚点：`run-2026-09-26T01-42-36-986Z` 里注入⑧ 的 `e2e-i-…-dqy5zx` 在 A 侧日志有 **132 行**，
报告摘出的行里含它 **0 行**，`trace.transfer_id` = `e2e-x-…`。
修法：过滤键改成按**形状**匹配（本轮所有 id 都是 `e2e-…<本轮 ISO>-随机` ⇒ 用 `e2e-\S*<ISO>` 一条正则），
以后新增轮不必登记、也就不会再漏一次。
改后实测同一族（`--fault=recv-dir-rotted-lie`，`run-2026-09-26T07-35-07-452Z`）：
A 侧摘出 8 行**全部**含 `e2e-i-`、B 侧 6 行含 1 行，`-lie` 仍**恰好 1/24 报红**、契约判据 0 次误报。
⚠️ 一条如实留下的边界：报告里那个**标量** `transfer_id` 仍指着 J2 那一单（本轮主单），
"每单都有自己那条 trace"这句还没做到 —— 那是 §十六 最后一层意思，登记不粉饰。
**（这条边界已于同日 #79 收口，见下面那一格。）**

★ **#79 把上面那条边界收掉：报告从"只报一单"变成"本轮每一单都数得出来"（同日）**。
形态与 #70 是同一个坑的另一半 —— 报告只有一个标量 `transfer_id` ⇒ 多文件轮那三单在报告里
只有一单有名有姓，追故障时另外两单**凭空消失**。
- 判据先写（对**已落盘产物**判，不吃内存对象）：`trace.ids` 必须是数组，且**报告正文里出现的每个
  本形状 id（`e2e-…<本轮 ISO>-…`）都必须已登记**。按形状认而不点变量名 —— 理由同 #70。
- 生成侧配一个铸 id 注册表：14 个铸造点全部改走同一个入口（含反向轮那个故意不存在的 decoy，
  它同样出现在报告里 ⇒ 不登记就会让反向轮因契约不合格多红一次，把"红恰好落在断言上"那条证据搅浑）。
- **两面实测**（都在 `--fault=multi-file`，同一份判据）：
  正向 `run-2026-09-26T10-34-24-478Z` ⇒ 多文件轮全绿（条数只登记在验收矩阵顶部「轮次账」，
  这里不留数字 —— 散文里的总数没有守卫）、`trace.ids` 数得出那三张同名不同目录的单、
  契约 ✅、`exit 0`；默认轮 `run-2026-09-26T10-34-12-978Z` 同样全绿、`exit 0`。
  反向＝**只换判据的输入**：把一处铸造点从注册表旁路掉（id 值一字不变，只是不再登记）⇒
  `run-2026-09-26T10-32-38-373Z` 功能层照旧打印全绿而**报告契约判红 3 条、`exit 1`**

  ⇒ 这一格的红来自判据本身，不来自应用。
- ★ 顺带被这次反向抓到一处**归因错**：保留策略回写后的那次复判原来会**复述**首轮已报的 gap，
  于是"首次落盘就缺的那一格"被打印成"回写弄坏了产物"（同一条 gap 还出现两遍）。
  发现它的实跑：`run-2026-09-26T10-31-25-797Z`（3 条 gap 打成 6 条 + 一句假归因）。
  改成只报**回写新增**的那几条，并单独验了这一格：把回写改成真的丢字段 ⇒
  `run-2026-09-26T10-33-30-518Z` 报"回写弄坏了产物"+ **恰好 1 条**、功能层仍 24 条全绿。
  自证夹具同步扩到 14 格（新增"ids 不是数组"/"出现没登记的 id"/"多单全部登记"三格）。

★ **§19「网络·Relay」这一格今天被改判，根因不是"缺一台中继服务器"（#66 的前置调查）**：
这一格原文写"要一台活着的中继服务器 + 一条被封的直连"，**前半句是错的** —— 服务器就在本机
`~/Documents/code/gosslan-relay-server`（零依赖 Node，带 `selftest.mjs`，2026-09-22 移出主仓）。
真墙是两条代码级事实：`network/transport/relay.rs:104` 的 `pick_relay_targets` 会把 `connected` 里的
对端**整个跳过**，而 `connected` 来自 `relay_link_snapshot`（同文件 120-136 行）= `state.links` 里
**任何一条**非空链路、**不分通道**；同机两实例靠 LAN announce 一定连得上（刚跑的 4 个 run 目录里
`path=lan` 与 `path=routed` 同时稳定存在）⇒ 对端**根本进不了中继候选**，不是"拨了没成"。
仓库里也**没有**只关 LAN announce 的开关（`discovery_mode` 全仓不存在，只有 `commands/dev_diag.rs:261`
一个展示字符串；`GOSSLAN_AUTOSTART` 还强制 `network::start`）。⇒ 要造"只有中继"需要 root 防火墙或
第二台真机，两者都越过「不碰系统 / 单机」这条线 ⇒ 这一格按§十 / §二十二-11 改记**平台 Smoke**
（两台真实设备 + 一条被封直连），**不升 AUTOMATED**。§五 的 `LAN + Tailscale` 是这一块欠账的另一格。
**⚠️ 曾并列在这里的"§12.4 gossip 多跳"已于同日 #75 用第三个实例清掉，别再抄进这一格** —— 它不需要"非 LAN 出口"，
只需要第三个真实对端（那套地基现在已经在 `--round=gossip3` 里）。★ 顺带把 §七「发送方提前放弃」与「用户点取消」拆成两件事：后者要 IPC 调
`cancel_file_transfer`（内存 `cancel_tx` + `AppHandle` emit），零改产码的 harness 够不着 ⇒ 取消那一格
需要 WebView/WebDriver 驱动，或记 Smoke；**不许用前者冒充后者**。

**注入⑨「停滞轮」（#66，2026-09-26）**：§七「发送方提前放弃 / 接收方仍在线」× §19「文件·断线」
这一交叉此前**零跨实例判据**。时序：判据自己入队 → 自旋抓到 `.part` 真的在长（动手时刻实测在 1.8 MB / 7.1 MB
之间摆，取决于自旋撞上 `SIGSTOP` 的时机 ⇒ 这个数只打印不设判据）
→ `SIGSTOP` 接收端 → 冻 70 s → `SIGCONT`。三条实测结论：
① ★ 不变量「接收端真实进度」第一次有跨实例机器证明 —— **冻结期间盘上字节一字不涨**
（7,077,888 → 7,077,888），终名文件不存在、两侧台账都不是 done。发送端灌进 socket 与内核缓冲的字节
没有被算成"已收到"。
② ★ **我原本要钉的那条被判据自己否掉了**：先兑现的出口是 chunk loop 的
`[FILE] COMPLETED … ok=false`（队列行退回 `pending`、`attempts=1`），**`[STALL]`（60s 停滞放弃）一行都没出现**。
⇒ 这一轮只把"走的是哪条放弃出口"当打印信息；写成判据就是拿一条未兑现的路径许愿（假红形状）。
③ 解冻后确实按真实进度补完：sha256 等于源、两侧 done、队列清零、半截 `.part` 不残留；
但**解冻 → 落地实测 5.5 s / 28.6 s / 51.6 s 都出现过**（原链路直接续 vs 先退避再重拨），
抓到在飞的字节也随机器负载在 1.8 MB / 7.1 MB 之间摆 ⇒ 恢复是**有界**的，
但"多快"这一格今天仍没有判据（把随负载摆的量写成常数就是第二个事实源）。
④ ★ **这一轮自己那条"终局"判据被本地层跑红，红的是同步点选错了**：判据拿"B 的终名文件出现"当
A 该终态的时刻，可 A 的 `done` + 删 `file_outbox` 行是**收到 `FileCompleteAck` 之后**才写的，
而那条 ack 在 rename **之后**才发 ⇒ 天然顺序就是"先见 B 落地、后见 A 终态"。
本地层第 12 步实测：解冻→落地 5.5 s 的那一刻读 A ⇒ `{A:active, outbox:1}` 判红，
而同一步里 B 已 done、字节与 sha 全对、无 `.part` 残留 ⇒ **不是产品分叉，是判据读早了**
（单跑一次绿纯属运气；`-lie` 轮打印出 **B 落地 → A 终态 14.6 s**，量出了这个天然滞后 ——
而改完之后同一格又打印出 **0.0 s** ⇒ 这段滞后**不是常数**，"这次赶上了"完全不构成可靠）。
修法：`waitSendTerminal()` —— 有界等发送侧自己走到终态（60 s = 心跳 5 s + 队列退避 5 s 留余量，
到不了才判红），耗时打进报告。**同一形状顺着 helper 把 harness 扫了一遍**：
默认轮 J2 / 脏前缀轮① / 续传轮② / 杀进程轮⑥ / 冻结轮④ / 停滞轮⑨ 全部换用同一个 helper
（① 那两条是蕴含式后件 ⇒ 只在"补齐"那一支才等，`-lie` 模式走的还是不等待的老路径，反向自证不受影响）
—— 其中 ⑥ 原本用 `sleep(15_000)`、② 用 `sleep(12_000)` 把这类竞态**藏住**而不是解决它。
`check(` 调用点一条没增减（判据 C 现算复核仍对得上）。
⇒ 通用一条记进§十四：**跨实例判"终态"之前先问"这一侧的终态是被谁点亮的"**，对端产物不是本端的同步点。
★ 同一族的**另一半**（2026-09-26 由注入③ 自己判红一次才照出来，记进§十四）：**注入式判据的"基准读数"必须取在注入动作确认生效之后** ——
`.part` 这类只有被注入进程会写的文件，进程一死/一冻就永久定格，所以"确认已死之后的那一次 `stat`"才是可比基准；
取在按下 `SIGKILL` **之前**的那一次，中间隔着"B 还在收片 + 写入尚未在 `stat` 上显现"这扇窗，
等式判据就会**偶发地把判据自己的读数竞态报成产品的错**（实测差恰好一片 262,144 B：快照 4,194,304 vs 冻结 4,456,448）。
两条配套纪律：① 问句从"这个数是靠谁定格的"出发，`SIGSTOP` 还要记得**信号是异步生效的**（紧贴调用去读仍可能读到最后一片没落地的瞬间 ⇒ 留一句 settle）；
② **"窗口有没有真打中"和"产品有没有服从盘上真值"是两件事，不许共用一个数** —— 前者按决策时刻的快照判，后者按冻结后的真值判，两个读数都打印出来当探针。
反证：`--fault=stall-mid-lie` 只换"补完该等于哪个摘要"这一份输入。
⚠️ 与④ 的分工必须说清：④ 先冻再入队 ⇒ 那一轮**根本没有在飞字节**；④ 注释里那句"在飞窗口等不到"
写的是它当时的掐时机方式，自旋抓法已被 ③/⑨ 两次证明做得到 —— 那句话已在 harness 里就地标注更正。

**注入⑩「发送端被杀轮」（#67，2026-09-26）**：§七「发送过程中杀进程」×「重启后继续」+ §八「本端重启」
+ §九「数据生命周期：运行→退出→重新启动」的交叉格。**先记一条我自己写错的账**：本轮之前纸上写着
"§七 只剩三实例并发"，而「发送端自己被 SIGKILL」那一格当时**也没有**判据 —— 注入③ 杀的是**接收端**，
全仓没有任何一轮从发送端崩溃这一侧看过。⇒ 凡是"只剩 X 格"这种**总数型**结论，落笔前要先按格重数一遍
（同一句话在本轮已错过两次，见 §十四）。

★ **这条判据钉的核心不是"能不能续传"**（②/③ 已证），而是**一次崩溃不许把「已入队」这个事实抹掉**：
`file_outbox` 那行是「先入队再投递」这条不变量的载体，它若因发送端崩溃变成 `done` 或直接消失，
这一单就**永久没人再发**，而 UI 上的气泡还在 —— 最难发现的一种丢法（现场没有任何东西会提示你）。
时序照 ⑨ 的同族形状：判据自己入队 → 50 ms 自旋抓到 B 的 `.part` 真的在长 → `SIGKILL` **A** →
死透后再等 20 s（这 20 s 是"发送端根本不存在"的窗口，也是这一格与 ③ 唯一的方向差别）→ 重启 A →
让它自己补完。实测：在飞 `.part = 5,242,880 B` 时杀，那 20 s 里 B 侧无终名文件、B 台账 `failed`、
A 台账 `active`、**A 队列行仍是 1**；A 重启后 **12.6 s** 从盘上那点字节续到整份 104,857,600 B、
内容摘要等于源、两侧 `done`、队列清零、不留 `.part`。
证据：正向 26 条全绿；`-lie` 只换"补完该等于哪个摘要"这一份输入 ⇒ **恰好 1/26 红**，
红的就是被换掉语义那一条，其余 25 条（含"两侧 done + 队列清零""无 `.part` 残留""崩溃后气泡还在"）照常过
⇒ 这条红不是顺带塌的，是精确落在判据上的。零生产码改动。
⚠️ 一条**新用法**留下的边界：这一格要在**进程活着时**写它自己的库（为了控制入队时机），
而 `seed()` 末尾那句 `wal_checkpoint(TRUNCATE)` 撞上在写的连接会 `SQLITE_BUSY` ⇒ 入队那步做了有限重试。
**这只是判据夹具的取巧，不许推广成通用做法**（真进不去就该换成"停机入队 + 更大文件"那条路）。
⚠️ 终局那条照⑨ 的教训走 `waitSendTerminal()`：A 的 `done` 由对端 ack 点亮，B 落地不是 A 的同步点。
★ **入队要写三行，不是只写队列那一行**（提交之后**同一轮内**补的加强；性质是"证明今天的行为正确"，
**不是修了一个缺陷**）：上面那段动机举的用户现场是"气泡还在" —— 而**「队列没了」和「气泡没了」在 UI 上是
两个不同结局**（前者=永久没人再发、界面上至少还看得见这一单；后者=用户连"曾经发过这一单"都看不见）。
只 seed `file_outbox` 一行就只能证明前一半，所以照⑥ 已有的形状把 `messages` 气泡行 + `file_transfers`
台账行一起补上，崩溃窗口各钉一条 ⇒ 25 → **26 条**。
★ **它的非空转证据（提交后补判据也要照旧规矩走）**：只把种下去的那个气泡 `msg_id` 换成 decoy
（**判据读的输入变了、判据本身没动**）⇒ 恰好 **1/26 红**、红的就是"崩溃后气泡必须还在"那一条；
跑完把文件按字节还原（`diff -q` 通过 ⇒ 那次红不会污染将提交的代码）。

★ **判据 D 当场抓到我自己的一处登记错误**（值得留在地图上，因为它不是笔误而是**结构**）：
我把反证轮名 `--fault=…-lie` 写进了 `verify.mjs` 那一层步骤的 `why` 文本里，守卫判红
——「local 层里点名了反向模式：它们**预期红**，进门禁就是把整层静音掉」。
判据 D 扫的是**整段字符串**（不分代码还是注释），所以"顺手在说明里提一下反证怎么跑"这条路本身就会被禁掉。
修法改的是我：反向模式只登记在 `package.json` 与验收矩阵的「轮次账」，并在那步的 `why` 里写死这条约束。
⇒ 通用一条：**禁令要写成"能被机器扫到的形状"，不是写成"文档里的一段话"**。

**轮次账的账（#64，2026-09-26）**：加一条判据 ⇒ 每一轮各多 1 条，判据 C 当场把 **12 处**手写数字判红。
这不叫"判据太严"，叫**文档结构在替代码还债**：同一个数抄在 11 段叙述里，它就是第二个到第七个事实源。
已按判据 C 自己给的办法处理 —— **把数字从叙述里删掉**，收成验收矩阵顶部一处「双实例 E2E 的轮次账」（10 行），
连带删掉「lie 恰好 1/22 报红」这类**分母**（红几条由当时翻掉哪个期望值决定，写成常数必漂）。
⇒ 以后加一条断言要改的只有那 10 行，其余行子只许写"按设计会红"。

**判据层自己的账（#63，2026-09-26）**：这一层的"自动化"以前只算了**被测产品**的账，没算**判据本身**的账。
今天补上：harness 拿日志行当就绪/投递证据，而应用每 append 一次就量一次文件大小、超 512 KB 就把整份
`x.log` 改名成 `x.old.log` 并**覆盖上一份旧档**（`logging.rs:296-324`），日志又跨启动追加、harness 从不清档。
⇒ 攒到临界点后，「等 boot 完成行」这类判据会在轮转瞬间看不见那一行。两次红都是**实测**：
① 默认读法（只看当前档最后 60 行）红；② 改成"跨两份文件数次数、和启动前的次数比"**仍然红**，
因为轮转会把上一份旧档删掉 ⇒ 计数**变小**。真修法 = 每次 launch 前把该实例的历史日志整份
**移进 run 目录**（`stashLogs`，移动不删、证据可查），判据读的那份文件从本次启动起归 harness 所有。
判据层单独成文件 `scripts/e2e-logtail.mjs` + 5 格夹具自证，**每轮 E2E 启动前先跑它**（红了就以「判据坏了」退出，
不再留一个 20s 超时让人猜）；独立入口 `npm run test:e2e:logtail-selfproof`。
非空转由三次变异证明：把读法改回今天的写法 → 红 2 格；去掉基线比较 → 红 1 格；把 `stashLogs` 改成空操作 → 红 3 格。

### 12.8 文档 —— ★ 本轮抓到"今天仍然不一致"的一组

| 契约 | 态 | 依据 |
|---|---|---|
| 代码 = 架构图 | ✅ | `check-domain-map.mjs` + `check-domain-deps.mjs` 在快速层 |
| 代码 = API 契约 | ✅ | `check-api-contract`（注册命令逐条对表） |
| 代码 = 事件契约 | ✅ | `src/api/events.test.ts`：扫 Rust 侧 `emit(` 的**事件名**与前端监听对账，且先按语法边界剥注释（它自己带一条"不被注释骗"的夹具） |
| 代码 = 不变量 | ✅ | `check-invariant-hooks.mjs`，26 条全部具名（0 条 NONE） |
| 代码 = 测试清单 | ✅ | `check-test-manifest.mjs` + 三份平台基线（缺=红、多=警） |
| 文档数字 = 实算 | ✅ | `check-doc-numbers.mjs` |
| **代码 = DB 契约** | ❌ **不通过** | 见下 |

**DB 契约这一格不是"没有门"，是"门本来就不可能有"**：`src-tauri/src/schema.sql` 与运行时真源
`db.rs` 的 `SCHEMA` 是**两份手写 DDL**，而生产码只读后者（全仓 grep `schema.sql` 的命中只有
`db.rs:377` 一句"与 `schema.sql` 保持一致"的注释、`domains.data.mjs` 的文件清单、
`check-change-budget.mjs` 的敏感文件名单 —— **没有任何代码 `include_str!` 它**）。
本轮按 §10 那两条 grep 实测：**两边表名集合不等**，文档版缺 `favorites` / `group_files` /
`group_file_recipients` / `group_recalled_messages`（复跑命令见 §10 末）。
⇒ `schema.sql:1` 自称"与 db.rs 保持一致"是**假陈述**；它同时是 `check-change-budget` 眼里的 L3 敏感文件，
于是"改一处必须同步改另一处"这件事**全靠人记得**，而 §13 的原始教训正是"靠人记得一定会失守"。

**顺带一条二阶漂移**（记录它是因为它讽刺得很典型）：
`docs/ARCHITECTURE-EXPLAINED.md:463` 用来描述这条漂移的那句话本身已经过期 ——
它写"缺 `group_files`、`group_file_recipients` 两表"，实测是**四张**。
**描述漂移的句子也会漂**，所以终审表里凡是"N 张/N 条"都必须挂命令，不挂命令的一律重跑。

### 12.9 结论：现在**不能**宣布"最终稳定版"达成

不是因为还有一堆 bug，而是因为 §19 自己要求的三条里有三条不成立：

1. **文档 = 代码 不成立**（12.8 DB 契约那一行是实测反证，且 `schema.sql` 仍在被两个门当成"真源之一"）。
2. **网络分组不成立**——但缺口比上一版记的**窄**，这里改成实测口径：
   真正的洞是"**没有任何一次跨实例、且 LAN 不可用的真跑**"。
   ⚠️ 这里上一版写的是"Routed/Tailscale **连判据都没有**"——**已被同一张表 §12.3 那行的现算推翻**
   （选路层有 13 条具名单元判据：三条链路优先级、健康度/插入序不翻转、"只有 routed 时仍去拨 LAN"、配置解析四族）。
   **判据厚 ≠ 真跑过**：写反方向的这句话会让人去做一件重复覆盖的活，所以按实测口径改在这里。
   Relay 同样"判据很厚、缺的是经中继真通一次"（需要一条被封的直连，属环境）；BLE 只能真机。
   ⇒ §19 把这三条与 LAN 并列要求，所以这一组仍算不成立。
3. **UI 分组只做到了结构级**：运行时观感一层完全空白（12.6）—— 2026-09-26 已把"**为什么**空白"量清：
   不是没写断言，是这一层**没有 DOM、也没装任何测试框架**，而 `src/api` 的门面只有 Tauri 一条传送带
   （三条可选路与各自代价见 §12.6 末 ⇒ 待拍板 **#78**）。

要收尾，下面这四件事当时都算“必须做”，其余是“可以延后” —— **其中两件已经做完**（见各自条目的 ★），还剩两件各自卡在外物（一件等拍板、一件等环境）：

- **M-1′（原 A-8，需拍板，大白话）**：那份"给人看的建表脚本"和"程序真正执行的那份"**已经是两份东西**，
  而且缺 4 张表。两个选择：**(A) 让前者由后者自动生成**（永远一致，代价：那份文件不再手写、不再当文档读）；
  **(B) 直接退役前者**，把"表结构只看程序里那一份"写进文档。**推荐 B** —— 它不需要新增门，
  少一份文件就少一次失守；A 要新增一条"生成 + 比对"的判据，收益只是留一份好看的文档。
  ⚠️ 这不是"顺手能改"的：那份文件被两处护栏当成敏感文件/域清单成员，退役要一起改名单，故等一句确认。
- **网络缺"非 LAN 出口的跨实例真跑"**：需要先有"第二台机器 / 第二张网"的条件，属环境而非代码 ⇒ 保留 ⚠️/🔶 不硬凑。⚠️ **措辞修正（2026-09-26 第二次）**：这**不等于**"非 LAN 没有判据"——选路/优先级/配置解析那一层 **Routed 有 13 条具名单元判据**、Relay 的判据也很厚（见 §12.3 / §12.4 现算命令）。缺的只是"真跑一趟"，不是"没写判据"；这两句话差别很大：前者要环境，后者会让人去做一件重复覆盖的活。
- **★ §7 撞名那一格 ✅ 已做完（2026-09-26，#54，大白话）**：两份同名文件同时在半路时，原来后到的那份会**直接盖掉**先落盘的那份 —— 两条“已收到”都在，但其中一份的字节已经不在了。当时写在这里的“必须用户拍板（两份都留下 / 只留第一份）”已拍板＝**两份都留下、第二份自己换个名字共存**；而“要三个实例才能验”这个前提被 RCA 证伪 ⇒ 判据直接钉在生产收尾码上（先红：两行报同一个路径；修后绿）。见 §12.9 / A-3 行。

- **★ 第四件 ✅ 已做完（2026-09-26，#59）**：**"迁移中途失败"这一格已补上判据**
  （见 §12.2 标 ★ 那行）。事务机制今天**已经在**（`db.rs` 五处步骤事务），现在它**也被证明过真的回滚**：
  `a_failing_migration_step_rolls_back_completely_and_never_advances_the_version` 钉住
  「失败上抛 / 版本停在旧值 / 连 `ALTER TABLE` 一起回滚 / 原行不丢 / 拆掉注入源重放能续」。
  那句"验证 rollback 后仍存在"的**假注释**（函数体只验正常路径 ⇒ 注释说的比代码做的多）也已改掉。
  这一格当初是唯一的"**会导致用户数据丢失、又不需要谁点头**"的必做项 ⇒ 它已清零；
  同一天顺带照出的 #60（v7 真丢数据）也已修，剩下的"要不要清存量"退化成 **#61 一个点头**。
- 12.3 / 12.4 / 12.6 里那些 ⚠️ 与 🔶 不需要现在解决 —— 它们的正确处置是
**留在 Smoke 清单上并标成"未自动化"**，这正是 §10「绝不能把'没有测试'伪装成 PASS」要的形状。
