# 版本号规则（从 2026-09-12 起强制执行）

> 结论先说：**未发布的一批提交里最高档是什么，这次发布就提升哪一位**；
> 每个提交必须在提交信息里声明自己的档位（`Version-Bump:`），由 `npm run version:check` 把门。
> 依据：SemVer 的"一次发布取最高档" + conventional commits 的类型约定。

## 1. 三档判据（小 / 中 / 大）

| 档位 | 版本变化 | 什么算 | 例子 |
|---|---|---|---|
| **小**（patch） | `x.y.Z+1` | 缺陷修复；非功能性改动（docs / test / chore / build / ci / style）；小幅无行为变化的优化 | `fix(settings): 主题点了又跳回去`、`docs: 补注释`、`test: 补护栏` |
| **中**（minor） | `x.Y+1.0` | 一个完整的新能力，用户可感知的改进（`feat`、`perf`）；向后兼容 | `feat: 跨网段连接 UI`、`perf(logs): 隐藏时不再每 2s 拉日志` |
| **大**（major） | `X+1.0.0` | **向后不兼容**的改动（提交带 `!`，或正文含 `BREAKING CHANGE:` footer） | `feat!: 移除旧握手协议`、`refactor(db)!: 改存储结构且不迁移` |

判据是**确定性**的（同输入必同档），实现在 `scripts/semver.mjs` 的 `classifyCommit()`：

1. 提交带 `!`，或正文含 `BREAKING CHANGE:` footer ⇒ **大**（SemVer：MAJOR = 向后不兼容）；
2. `feat` ⇒ **中**；`perf` ⇒ **中**（向后兼容的新能力 / 改进）；
3. 其余（fix / docs / test / chore / build / ci / style / refactor / 未分类）⇒ **小**。

> ⚠️ **改动规模与线索词不参与定档**（2026-09-15 修订）：一个几百行、名字里带
> 「协议 / 传输 / 蓝牙」的**向后兼容**功能，仍然只是 **minor**。按体量升级会把兼容
> 功能误报成 MAJOR，与 SemVer 2.0.0 不符（MAJOR 的唯一含义是「向后不兼容」）。

## 2. 怎么"累加"：两个口径，别混用

- **逐提交累加**（字面执行"每次提交进一位"）：`npm run version:classify` / 台账里的"累计版本"列。
  仅用于**审计**。把这套规则套到历史 backlog（184 个提交、其中 23 个大）会算出 `25.1.2` ——
  它既不表达"这次发布有多大"，也和安装包/后端的版本语义脱节，所以**不用它定版本**。
- **一次发布取最高档**（SemVer 标准做法，**实际采用**）：`requiredLevel()` + `version:release`。
  同一批 backlog 的最高档是 `major` ⇒ 本次发布 `2.1.2 → 3.0.0`。

## 3. 日常怎么做（每个提交都要做）

1. 按上表给自己的改动定档；
2. 提交信息末尾加一行 **`Version-Bump: patch|minor|major`**（这就是"版本号提升约束"的落点：
   它让每个提交都被记账，而不是靠人记得去改版本）；
3. 提交前跑 **`npm run version:check`**：它会
   - 从**上一次版本提升提交**起，收集所有提交并计算最高档；
   - 若 `当前版本 < bump(当前版本, 最高档)` ⇒ 失败，提示你跑 `npm run version:release`；
   - 若某个提交缺 `Version-Bump:` 或写错档位 ⇒ 失败并逐条指出。
4. 攒够一批（或要发版时）跑 **`npm run version:release`**：按最高档一次性提升
   `package.json` / `Cargo.toml` / `tauri.conf.json` / `package-lock.json` 并落 CHANGELOG 版本小节。

## 4. 现在的台账与数字

- 全量分类台账：`docs/version-ledger.md`（184 个提交：**23 大 / 45 中 / 116 小**，由 `npm run version:ledger` 生成）；
- 逐提交字面累加：`2.1.2 → 25.1.2`（仅审计口径）；
- **本次实际发布：`2.1.2 → 3.0.0`**（最高档 = 大：多窗口架构重做、Mesh/中继协议分层、Android BLE 外设支持等）。

## 5. 2026-09-15 修订：MAJOR 只留给「向后不兼容」

原来的第 2 条（线索词 + ≥150 行 ⇒ 大）已删除：它会把**向后兼容**的大功能误报成 MAJOR，
与 SemVer 2.0.0 不符。**MAJOR 的唯一含义是「向后不兼容」**（提交带 ! 或正文含
BREAKING CHANGE: footer）。改动规模只影响 review 成本，不决定版本位。

> 影响：docs/version-ledger.md 是**按旧规则**生成的历史台账（保留作审计），
> 新提交按本页现行规则判定。4.3.x 里被记成「大」的兼容功能，按新规则应为 minor。

### 发布节奏（对「大厂 / 互联网最佳实践」的补充）

- **语义**：major.minor.patch（SemVer 2.0.0）已是业界标准，本项目不变。
- **节奏**：优先**攒批发布**（[Unreleased] 累积，按「这批最高档」一次提升），
  而不是每个提交都发一版。逐提交发布会让 patch 位膨胀（本仓库一天内 4.3.11 → 4.3.22），
  版本号就不再表达「这次发布了什么」。
- **内容传输（ADR-0019）是新的用户可感知能力 ⇒ 落地那批按 feat 记 minor，发布为 4.4.0**。

---

## 6. 豁免：V5 大改造迭代（用户 2026-09-15 裁定）

- **背景**：V5（LAN + BLE 无感融合，见 `architecture/README.md`）是一次**全新迭代 / 大改造**，不是小修小补。
- **规则**：本迭代内**不要求**逐提交 `Version-Bump:`；`npm run version:check` **不作为门禁**
  （会因缺少声明而报红，属预期，不阻塞开发）。
- **收口**：本迭代完成后**一次性定为 `5.0.0`**（正式版）。
- **恢复**：V5.0 之后的小修小补**恢复**本页 §3 的逐提交规则。
- **备注**：既有 docs-only 提交 `63873ac` 缺 `Version-Bump`，按本豁免**无需处理**。
- **边界**：本豁免只针对 V5 迭代；放宽不改变 `5.0.0` 的 SemVer 含义（向后不兼容的架构级变更）。
