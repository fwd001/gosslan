//! 存储与缓存子系统：Telegram 风格的轻量化存储与自动清理。
//!
//! 原则：
//! - SQLite 只存消息文本、密钥、好友 / 节点关系，**不存二进制大对象（BLOB）**。
//! - 图片、音频、文件等二进制全部落盘到 `Cache` 目录，由前端异步懒加载。
//! - 后台闲置时自动清理过期缓存，并对 SQLite 执行 `VACUUM` 整理碎片。

pub mod cache_cleaner;
